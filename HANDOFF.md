# HANDOFF — SBC Guest Management Portal (Laravel 12 build)

> **Audience:** a developer (or AI assistant) joining this repo mid-build.
> **Read time:** ~10 min to orient, ~30 min to feel competent.
> **Last updated:** end of **Phase 09b verifier + route-registration polish round (2026-07-27)** — see § 1 for build state. **All 11 PRIMARY + 3 sub-phase bundle verifiers GREEN** at 261/261 assertions. **Polish round discovered + fixed genuine source bug**: routes/web.php was missing the `POST /notification-settings/test-email` route that HANDOFF.md Phase 09b row claimed was shipped — controller method `NotificationSettingsController::testEmail()` had been added + Settings.tsx had rendered the Send test email button, but no route registration existed (would have silently 404'd in production). Added the missing route inside the existing `auth` middleware group, restoring full 4/4 notification-settings route surface (index/store/destroy + test-email). Phase 09b verifier [12] assertion caught this gap — restated as polish-round testimony: multi-substring named-route assertions (Route verb + URL + method + name) MUST NOT be softened in future verifiers. **Verifier rewritten to str_contains-only (15 source-pattern assertions)**: replaces prior regex-with-escape-minefield design that broke twice (round 1 escape-minefield + round 2 `$this`-in-global-scope fatal). **Production-push fully unblocked**: `tsc --noEmit -p tsconfig.json` returns 0 errors; `npm run build` succeeds; `public/build/manifest.json` + `public/build/assets/` (1 CSS + 51 JS) populated. **All 12 shipping phases are complete**: Phase 02-07 + 06d.x + 08/09/10/11/12 + 06b/06c. **No next shipping phase remaining**; the project is feature-complete and deploy-ready per `Implementation/Phase_12_Deployment.md`. **Rolled-over deferred items** (Phase 09b + 10b + 11b) captured in §1 Phase 09/10/11 row footers — non-blocking, scheduled for a separate polish round. **`Phase 06d.0` ships today (22/22 sub-assertions green)**: brand-new AdminSidebar (13 nav entries, 260↔72px collapsible) + AdminDashboardLayout (sidebar + topbar w/ Global Search placeholder + LanguageSwitcher stub + footer skeleton) + Greeting + Sparkline (inline SVG, NaN-safe) + AnimatedCounter (rAF + prefers-reduced-motion) + KPICard extended (`series?` + `sparkTone?` + `animateValue?`); Dashboard.tsx parent restructured to use AdminDashboardLayout only for `variant === 'admin'`; AdminDashboard function now renders 7-card horizontal row of KPIs with per-KPI delta + sparkline + AnimatedCounter; 5 admin stub Pages (`Admin/{Users,RolesPermissions,Messages,Analytics,Submissions}/Index.tsx`) — 4 placeholders + 1 functional alias; 5 NEW admin sidebar routes; DashboardController::adminDashboard extended to return `kpis + kpiDeltas + kpiSeries` (last 7d vs prior 7d delta + last 14d daily sparkline buckets, SQLite-compatible via PHP-side bucketing); `scripts/verify_phase06d0_run.php` is the NEW verifier (22 sub-assertions across 7 sections: layout/sidebar primitives → stub pages → routes → controller → Dashboard.tsx integration → sparkline/AnimatedCounter risk-mitigation → regression checks); Phase 02-07 regression net re-ran clean at **58/15/36/22/14/27/21 — no regression**. **Reviewer-driven bugfixes applied mid-ship**: (1) verifier [14] Laravel `collect()` helper undefined → replaced with `foreach` + `str_contains` per route name; (2) `AdminDashboardNew` rename + `usePage<AuthLikeProps>()` reads `auth.user.name` so Greeting shows the actual admin user not literal "Administrator"; (3) Submissions stub back-button text aligns with the 4 placeholder stubs ("← Back to Dashboard") so verifier [13] back-link check passes. **`Phase 08 (Leadership Board UI)` shipped today (2026-07-27, 19/19 sub-assertions green)** — `LeadershipBoardController::index()` (Stacked-multi-board Inertia view: Admin sees all primaries; Impact_Leaders column-level sub-cell assignment gate filters to user's assigned primaries only — data-leak fix) + `show()` (single-board JSON endpoint per `cellId`) + private `buildBoardData()` helper (RoleHelper-aware auth gate: Admin / Impact_Cell_Admin / Impact_Cell_Report; report-status logic `Submitted`/`Pending`/`Overdue`/`New` by days since last report; tiles sorted by status desc, members desc); `routes/web.php` `GET /leadership` registered (joining the existing `GET /leadership-board/{cellId}`); AuthenticatedLayout nav entries for "Leadership Board" appended to BOTH Admin branch (after Audit Log) and impactCell branch (after My Reports) — label-derived testid `nav-leadership-board` per Phase 06c; `LeadershipBoard.tsx` extended with `initialData?: BoardData` prop + `useState<BoardData|null>(initialData)` skip-fetch guard + `useEffect` fetch fallback; `Pages/Leadership/Index.tsx` loops `boards.map` and passes `initialData={board}` to skip N+1 AJAX; `scripts/verify_phase08_run.php` maintains the 19/19 baseline assertion count — Phase 02-09 + 06d.0/06d.1/06d.2 + 06c regression net unchanged. `**`Phase 10 (CSV Import/Export)` shipped today (2026-07-27, 19/19 sub-assertions green)** — `CsvImportController::index()` admin-gated Inertia `Csv/Import` page + `CsvImportController::import()` admin-gated + `['csv' => 'required|file|mimes:csv,txt', 'template' => 'nullable|in:officer,team,impact']` validation + `str_getcsv` parser per spec + 5-alias `phone` map + duplicate-by-phone via `Guest::where('phone', $phone)->exists()` + skip-row accounting via `{created, skipped, errors}` JSON shape; `CsvExportController::export()` RoleHelper::groupOf-gated for Admin + followUpOfficer-group + followUpTeam-group + private `columnsForRole('Administrator')` returning 14-column full-visibility set (all 3 group-owned column-sets) + fallback 8-column officer/team subset EXCLUDING `nearest_impact_cell_id` + `follow_officer_id` + `follow_up_status` + `follow_up_contacts`; 3 routes registered (`GET /csv/import` + `POST /csv/import` + `GET /csv/export`); `resources/js/Pages/Csv/Import.tsx` bulk-import UI (drag-and-drop `data-testid="csv-drop-zone"` + hidden `csv-file-input` ref + `csv-upload-button` → `fetch('/csv/import', { method: 'POST', body: formData })` + result `data-testid="card-csv-result"` with `StatusPill` for Created/Skipped/Errors + collapsible error-accordion); `scripts/verify_phase11_run.php` — 19 source-pattern sub-assertions. **Project feature-complete** — no next shipping phase remaining; Phase 09b + 10b + 11b deferred-item follow-ups (Mail + SMTP + CSV + Reports polish) scheduled for a separate non-shipping round. **Verifier-green requires `php -l`-clean first** — `scripts/verify_all_phases_run.php` master runner does `php -l` on every `scripts/verify_phase*_run.php` BEFORE counting assertions (added in Phase 12 polish round; a verifier that cannot be parsed cannot pass).

This is the **build-state + next-steps** document. For the _design intent_ (what we're building and why), see `Plan/` and `Implementation/`. For the _technology mapping_ (which Laravel package / idiom replaces which old-stack idea), see `Implementation/00_Laravel_Bridge.md`.

---

## 0. TL;DR

We're rebuilding the SBC Guest Management app — a 3-user-group portal (Impact Cell, Follow Up Officer, Follow Up Team) for tracking "guests" through outreach → follow-up → integration — on a fresh **Laravel 12 + Inertia + React 18 + Breeze + Spatie Permission/Activitylog** stack. The MySQL 8.4 DB (`impact_guest` on localhost, user `ipcDBurs22`) is bootstrapped and live. **Phases 02 → 06 are green (58 / 15 / 36 / 22 / 14 sub-assertions all passing — see §1 for per-phase detail). Phase 05c follow-ups (guest edit form + controller `AuthorizesRequests` trait fix + source-of-truth Edit page + DROPPED-FROM-DRY refactor) also shipped. Phase 06 adds the Team Dashboard with 4 team KPIs, inline follow_up_status editing, Contact Sections timeline, and View-Only mode for Follow_UP_View_Only. **Auth UI polish prelude is now shipped** — split-screen brand panel (indigo gradient + animated glow orbs + SVG grid pattern + Heroicons feature pills), show/hide password with `type="button"` (Enter-key safe), envelope/lock icon prefixes, `kbd ↵` keyboard hint, polished CTA, and `/public/logos/{logo,logo1}.png` asset hosting. **`Phase 06b+06c — Foundations + Chrome + Page-pass refactor` shipped (2026-07-27, 27/27 sub-assertions green)** — design tokens, AuthenticatedLayout chrome polish, `KPICard` / `StatusPill` (brand tone red→indigo) / `EmptyState` primitives aligned to the token system, and bulk refactor of 38+ arbitrary-value `motion-safe:animate-[fadeIn_...]` usages across `Pages` onto the new tokens. `Phase 07 (Impact Cell Leader forms)` shipped today (2026-07-27, 21/21 sub-assertions green) — sidebar gain, leader dashboard guest list + inline impact_status pill editor. **`Phase 06d.0 — Admin Dashboard polish (prelude)` shipped today (2026-07-27, 22/22 sub-assertions green)** — admin-only sidebar (13 entries — 6 NEW stubs + 7 existing), admin-only topbar (Global Search input placeholder + LanguageSwitcher button placeholder), admin greeting block + 7-card horizontal KPI row with per-KPI delta + sparkline + animated counter, and 5 NEW admin stub Pages. **`Phase 06d.1 — Admin Overview Analytics (recharts + DateRangeFilter)` shipped today (2026-07-27, 15/15 sub-assertions green)** — eager HeadlessUI Popover DateRangeFilter (4 preset buttons + Custom popover with from/to validation), lazy-loaded recharts AreaChart (4 cumulative metrics: Guests/Contacts/Submissions/Users over Today/Week/Month/Year/Custom), controller `parseRange` + `buildChartSeries` + `seriesForRange` helpers + 5 new payload keys (rangeKey/rangeFrom/rangeTo/rangeLabels/chartSeries). **`Phase 06d.2 — System Overview + Recent Activity + Recent Registrations + Footer + wired GlobalSearch + wired LanguageSwitcher` shipped today (2026-07-27, 15/15 sub-assertions green)** — 4 progress-bar cards (DB size try/caught for SQLite compat + Storage + Active Users + System Health), 6 colored-icon Recent Activity tiles (Guests/Cells/Reports/Notifications/Audit/Users), mixed-source Recent Registrations feed (3 guests + 3 users + 3 submissions with avatar initials + RelativeTime helper), wired HeadlessUI Combobox GlobalSearch + Listbox LanguageSwitcher in AdminDashboardLayout + branded FooterCard in footer slot. **`verify_phase06d0_run.php` [2] updated** to assert the post-06d.2 architectural contract (component imports + usage, not literal placeholder testids that relocated into GlobalSearch.tsx:61 + LanguageSwitcher.tsx:48). **Phase 08 (Leadership Board UI) is still paused mid-ship** — controller + show + index + LeadershipBoard.tsx `initialData` prop + `Pages/Leadership/Index.tsx` all written, but routes + nav + HANDOFF still TODO. Next up: **resume Phase 08 ship** (last of the post-06 polish ensemble).

---

## 1. Build state

| Phase                                                                                 | Doc                               | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | ---------------------------------------------------- | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 00 Pivot                                                                              | —                                 | ✅ Done — pivoted from Node/Prisma to Laravel 12                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 01 Foundation                                                                         | `Phase_01_Foundation.md`          | ✅ Done — Laravel 12.64.0 + Breeze + Spatie scaffold, `.env` configured, `storage:link` created, all baseline migrations applied                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 02 Auth + Users                                                                       | `Phase_02_Auth_And_Users.md`      | ✅ **Done** — 9 roles seeded, `sbcadmin@impact.test` admin user, `RoleHelper` (3-group matrix — keys are now **snake_case** to match the wire format; see post-Phase-05 fix below), `User::activeRole()` accessor, `POST /auth/switch-role` route. **58/58 verification assertions pass** (was 40/40; added `[1b]` matrix-shape regression guard in the post-Phase-05 fix).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 02b **UI wire for Phase 02**                                                          | —                                 | ✅ **Done** — top-bar role badge + role switcher dropdown (`RoleBadge.tsx`, `RoleSwitcher.tsx`), `HandleInertiaRequests` shares `auth.user.{activeRole,roles,hasMultipleRoles}`, stub pages render without 500.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 03 Impact Cell hierarchy                                                              | `Phase_03_Impact_Cell_Model.md`   | ✅ **Done** — `impact_cells` table (UUID PK, `parent_cell_id` FK self `restrictOnDelete`, `is_primary`, `order`), `ImpactCell` model with `parent()` + `subCells()` self-relations + `hierarchyRulesOrThrow()` validator, `ImpactCellSeeder` (69 cells: 65 primary + 4 APO sub-cells), `ImpactCellController` (CRUD with pre-check + delete + `abort(409)` for destroy), 5 routes. **15/15 sub-assertions pass** (Phase 03 verifier impl has 7 numbered cases; `[2]` (count split) + `[3]` (hierarchy invariant) + `[5]` (controller 409 path) + `[6]` (idempotency) are all multi-check, hence 15 total. No Phase-02-style `[1b]` shape guard was added — coverage gained from earlier split-into-sub-checks refactors).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 04 Guest CRUD + column policy                                                         | `Phase_04_Guest_Records_Core.md`  | ✅ **Done** — `guests` table (UUID PK, `softDeletes`, `nearest_impact_cell_id` FK `restrictOnDelete`, `follow_officer_id` FK `restrictOnDelete`, 7 indexes + `follow_up_contacts` JSON), `Guest` model (`HasUuids` + `SoftDeletes` + `nearestImpactCell()` + `followOfficer()` + `scopeAssignedTo` + `scopeForImpactCell`), `GuestRequest` (`prepareForValidation` strips via `RoleHelper::stripDisallowed` — now correctly strips per snake_case matrix), `GuestResource` (admin-only timestamp masking), `GuestPolicy` (row-level by role+group), `GuestController` (5 routes + cross-cutting rule clearing `visitation_status`/`feedback` when `contacted_status != AvailableForVisit`), `ImpactCellPolicy` (Phase 03 follow-up: admin-only updates). **36/36 sub-assertions pass** (Phase 04 verifier has 20 numbered cases; `[6]`/`[7]`/`[8]` (Form Request stripping per role) + `[12]`/`[13]` (GuestResource masking per role) each split into ~3 sub-checks, hence 36 total. No new `[21]` shape guard in this phase — the snake_case-shape regression guard lives in `verify_phase02_run.php` `[1b]` only).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 04b **Local infrastructure**                                                          | —                                 | ✅ **Done** — parent `.htaccess` bridges Laragon's auto-vhost (`DocumentRoot=project parent`) into Laravel's `public/` entry. Unconditional rewrite for non-`/public/` paths (so `composer.json`, `app/*`, `routes/*` etc. are never servable). Belt-and-suspenders `RedirectMatch 404 /\.(?!well-known/)` + `Options -Indexes`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 05 Follow Up Officer dashboard + reassign permission                                  | `Phase_05_Follow_Up_Officer.md`   | ✅ **Done** — `DashboardController` with 5 KPIs (Pending Contacts / Total Calls / Visited / Pending Visit / Response Rate) for the FUp Officer group, top-8 queue, role-aware top-bar nav (Dashboard / My Guests / Profile when officer; admin keeps full nav), reusable `KPICard` + `StatusPill` components. `RoleHelper::canEditField` extended: `follow_officer_id` is now a Per-Role Special Case — only `Administrator` + `Follow_UP_Admin` may reassign (matrix self-assign otherwise handled in `GuestController::store`). Officer dashboard variant in `Pages/Dashboard.tsx`. **`FollowUpOfficerSeeder`** seeds `officer1@impact.test` (FollowUpOfficer, 5 guests) + `followUpAdmin@impact.test` (Follow_UP_Admin, can reassign). **22/22 sub-assertions pass.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |     | 05b **snake_case matrix fix (silent data-loss bug)** | —   | ✅ **Done** — `RoleHelper::GROUP_GUEST_OWNER` matrix changed camelCase → snake_case to match the production HTTP wire format (DB columns, migration columns, GuestResource output, frontend types all use snake_case). Without this fix, every multi-word field was being **silently stripped** from FollowUpOfficer / Follow_UP / Impact_Leaders writes — the strip policy was checking the wrong casing. Cross-cutting: the Inertia-prop boundary in `DashboardController` still camelizes (`'contactedStatus' => $g->contacted_status`) for idiomatic React/TS, but the policy operates on snake_case. Verified by 58/15/36/22 across all 4 phases plus a `[1b]` snake_case-shape regression guard that fails fast if a future contributor types camelCase in the matrix. |
| 05c **Phase 05 follow-ups: edit form + controller auth + column-policy FE/BE mirror** | —                                 | ✅ **Done** — 3 issues addressed: **(1) Controller auth**: `app/Http/Controllers/Controller.php` now uses `Illuminate\Foundation\Auth\Access\AuthorizesRequests` (Laravel 11+ starter scaffold ships an empty `Controller`; without the trait `GuestController::show/store/update/destroy` 500 on `Call to undefined method ...::authorize()`). Added to the **base** class so `ImpactCellController` benefits too. **(2) Edit form**: `routes/web.php` adds `GET /guests/{id}/edit → guests.edit`. `GuestController::edit()` is the source-of-truth rendering helper — computes `editableKeys` via a new private `computeEditableKeysForRole(?string $role): array` shared with `show()` (DRY), passes them + `editableFields` + `impactCells` to the Inertia `Guests/Edit` page. `resources/js/Pages/Guests/Edit.tsx` is the new edit form: Inertia `useForm`-based + reactively conditional on `editableFields` so officer never sees input for `nearest_impact_cell_id` / `impact_status` / `guest_name` / `follow_up_status`. The `submit()` further **whitelists** the PUT body via `Object.fromEntries(Object.entries(data).filter(([k]) => editableFields.includes(k)))` — defensive FE-side mirror of `RoleHelper::stripDisallowed` so a server-side regression can't silently re-introduce the column-policy bypass. Optional enum Selects use a `withCurrent(options, currentValue)` helper that prepends the existing value as `value (legacy)` if it isn't in the canonical list — prevents silent data loss on legacy enum values. `Guests/Show.tsx` conditionally renders the Edit Link iff `editableFields.length > 0` — a viewer with no update perms can no longer click → 403. **(3) Test credentials table**: HANDOFF §2 now has the full 8-user seeded list with passwords + roles + creator. **All 5 phase verifiers still 58/15/36/22/14 plus a `[1b]` snake_case-shape regression guard.** |
| 06 Follow Up Team dashboard                                                           | `Phase_06_Follow_Up_Team.md`      | ✅ **Done** — `DashboardController` now dispatches `teamDashboard()` for `followUpTeam` group with 4 team KPIs (Pending Contacts / Contacted Today / Wrong Number / Not Reachable) + team queue (all guests, sorted by `follow_up_status` priority, with inline dropdown + "Mark Contacted" quick-set). `GuestPolicy::update` now allows `Follow_UP` (but not `Follow_UP_View_Only`) to update any guest (column-level restriction via Form Request). New `PATCH /guests/{id}/follow-up-status` JSON endpoint for lightweight inline status changes. `ContactTimeline` component renders `follow_up_contacts` as a vertical timeline (editable for Follow_UP in the Edit page; read-only for other groups in Show). `ViewOnlyBanner` component renders an amber warning when `activeRole = Follow_UP_View_Only`, with all controls disabled. `FollowUpTeamSeeder` seeds `team1@impact.test` (Follow_UP) + `teamViewOnly@impact.test` (Follow_UP_View_Only) + 5 marker guests covering all `follow_up_status` permutations. **14/14 sub-assertions pass.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          || 06d.1 Admin Overview Analytics                                                       | `Phase_06b-06c_UI_Polish.md`   | ✅ **Done (15/15) — 2026-07-27.** eager HeadlessUI Popover DateRangeFilter (4 presets Today/Week/Month/Year + Custom popover with from/to validation) → lazy recharts AreaChart (4 cumulative metrics Guests/Contacts/Submissions/Users) → controller `parseRange` + `buildChartSeries` + `seriesForRange` private helpers + 5 new payload keys (`rangeKey`/`rangeFrom`/`rangeTo`/`rangeLabels`/`chartSeries`). `scripts/verify_phase06d1_run.php` now 15/15 green after defensive regex tightening on `[14]`/`[15]`'s `$adminFn` extract (anchored on the unique `data-testid="admin-dashboard-root"` line + function-closing `\n\s*\}\s*\z` instead of greedy-to-EOF — safe against future file growth below `AdminDashboard`). |
| 07 Impact Cell leader forms                                                           | `Phase_07_Impact_Cell_Leader.md`  | ✅ **Done (2026-07-27, 21/21 sub-assertions green)** — 4-sub-phase ship: **(1)** `AuthenticatedLayout` `navItemsFor()` impactCell branch extended from 3→7 nav entries in spec order (Dashboard / Members Data / Submit Report / Childbirth Notice / Souls Registration / Soul Search / My Reports) — the 4 new `/impact-submissions/create?type=X` entries are direct href strings with placeholder `routeName` keys since each query param variant shares the underlying route. **(2)** `DashboardController::leaderDashboard()` now sources `kpis.memberCount` from a real `ImpactSubmission::where('user_id', $user->id)->where('type','member')->count()` (was hard-coded 0) and returns a new `assignedGuests` payload — `Guest::query()->where('nearest_impact_cell_id', $primaryCellId)->orderByDesc('created_at')->limit(20)`, projected to `{id, guestName, phone, impactStatus, createdAt}`. Also passes `canEditImpactStatus` (true when `activeGroup === impactCell`) for safe pill gating. The `LeaderDashboard` component in `Dashboard.tsx` picks this up with a new `<section>` rendering a name/phone/impact_status/edit-table — empty-state hint differs by `primaryCellId` presence ("anchor your cell" if not set). **(3)** NEW `GuestController::updateImpactStatus(Request, $id): JsonResponse` — mirrors the Phase-06 `updateFollowUpStatus` JSON shape. New column-level role guard positioned BEFORE `findOrFail` + `authorize('update', $guest)`: aborts 403 unless `activeRole === 'Administrator'` OR `groupOf(activeRole) === 'impactCell'` (the latter already covers `Impact_Zonal_Cordinator` per `RoleHelper::GROUP_IMPACT_CELL`). Validates `impact_status` as `nullable|string|max:64`, then coerces `''` → `null` so the DB column keeps a true three-state shape. Companion route `Route::patch('/guests/{id}/impact-status', ...)->name('guests.impact-status')`. NEW `resources/js/Components/InlineImpactStatusPill.tsx` — button-pill, outside-click-close dropdown with 3 status options + Clear, sends `null` (not `''`) on Clear, optimistic UI with rollback-on-error. **(4)** NEW `scripts/verify_phase07_run.php` — 21 sub-assertions across 7 sections (nav shape / routes + fillable / `DashboardController::leaderDashboard` extensions / `ImpactSubmissionController` shape [4-type dispatch + duplicate-prevention flash] / soul-search controller [`q<2` guard + `type='soul'` scope] / `InlineImpactStatusPill.tsx` / `GuestController::updateImpactStatus()` + a NEW `[21]` role-guard ordering check that fails if someone reverts the guard to AFTER `authorize`). **Reviewer-driven mid-ship fixes**: (a) FollowUpOfficer→impact_status write hole closed (GuestPolicy::update gates by row, not by column — the explicit `RoleHelper::groupOf() === GROUP_KEY_IMPACT_CELL` check is required for column-level enforcement); (b) empty-string-vs-null DB-write ambiguity closed server-side AND frontend sends `null` explicitly (cleaner at source). Phase 02-06b regression net re-ran clean at 58/15/36/22/14/27 — no regression. |
| 06b/06c **UI Polish — Foundations + Chrome + Page-pass refactor**                                             | `Phase_06b-06c_UI_Polish.md`        | ✅ **Both halves shipped (2026-07-27)** — (1) **Auth side** (prelude, 2026-07-26): `Login.tsx` + `GuestLayout.tsx` + `/public/logos/{logo,logo1}.png` split-screen brand panel (indigo→blue→indigo gradient with animated glow orbs, SVG grid pattern, Heroicons `Users / Phone / Star` feature pills), show/hide password w/ `type="button"` (Enter-key safe), envelope + lock icon-in-input, eye/eye-slash visibility toggle, `kbd ↵` keyboard hint, dark-mode parity, `role="status"` + `aria-hidden` accessibility nits, `!important` overrides on `PrimaryButton` for Tailwind source-order safety. (2) **Foundations + Chrome** (Phase 06b round): `boxShadow.card` + `boxShadow['card-hover']` with CSS-variable fallbacks for `.dark` parity (declared in `app.css` `@layer base`); `keyframes.fadeIn` + `animation['fade-in']`; `darkMode: 'class'` aligned with Breeze. AuthenticatedLayout chrome polished — dropdown-trigger uses the tokens, `<main>` uses motion-safe fade-in, redundant inline `<style>@keyframes</style>` block removed (Tailwind now emits the keyframe globally). `KPICard` / `StatusPill` (`brand` corrected red→indigo per Phase 06b Color System §1) / `EmptyState` primitives refactored to the tokens, `data-testid` values preserved verbatim. (3) **Page-pass refactor** (Phase 06c round): bulk-substituted `motion-safe:animate-[fadeIn_0.4s_ease-out]` → `motion-safe:animate-fade-in` (31 calls), `shadow-[0_4px_20px_rgba(0,0,0,0.03)]` → `shadow-card` (24 calls), `hover:shadow-[0_8px_30px_rgba(0,0,0,0.06)]` → `hover:shadow-card-hover` (3 calls) across `resources/js/Pages/**/*.tsx` (excluding `Auth/*` + decorative `Welcome.tsx` glow/pulse orbs). `GuestLayout.tsx` line 226 swapped to the token, inline `<style>{@keyframes fadeIn…}</style>` safety-net removed (Tailwind now emits `fadeIn` globally). `Guests/Edit.tsx` sticky-bar negative-offset shadow intentionally preserved as arbitrary value (`shadow-[0_-4px_20px_rgba(0,0,0,0.03)]`) with an explanatory comment — token semantics differ (sticky bar casts up; card casts down). **27/27 sub-assertions in `scripts/verify_phase06b_run.php`** (`[1]`-`[22]` tokens + chrome + primitives, `[23]` regex-anchored keyframe-block absence check with `\b` word-boundary + `/s` DOTALL, `[24]`-`[27]` GuestLayout + Pages-sweep zero-arbitrary form), all green; Phase 02-06 re-ran at 58/15/36/22/14 with no regression. `data-testid`s preserved on touched Pages — KPICard/StatusPill/EmptyState asserted via [11]/[14]/[17]; full-Pages sweep not asserted. Sidebar idea dropped (data tables need full width). |
| 08 Leadership Board                                                                   | `Phase_08_Leadership_Board_UI.md` | ✅ **Shipped (19/19) — 2026-07-27.** `LeadershipBoardController::index()` + `show()` + `buildBoardData()` (RoleHelper + Admin-or-impactCell-group fail-fast gate + Impact_Leaders column-level sub-cell assignment gate + `leaderFullName` deferred per users.impact_cell_id TODO); `routes/web.php` `GET /leadership` registered (joining existing `GET /leadership-board/{cellId}`); AuthenticatedLayout nav entries appended to **both** Admin branch (after Audit Log) and impactCell branch (after My Reports); `LeadershipBoard.tsx` extended with `initialData?: BoardData` prop + `useState<BoardData\|null>(initialData)` skip-fetch guard + useEffect fetch fallback; `Pages/Leadership/Index.tsx` loops `boards.map` and passes `initialData={board}` to skip N+1 AJAX. Stacked view of all primaries (Admin) / user's assigned primaries only (Impact_Leaders data-leak fix). **Phase 06d.0 [22] flipped** from absence-marker to "Phase 08 verifier present + ≥19 sub-assertions" (soft regression guard against accidental deletion); **Phase 07 [1]/[3] rebaselined** (impactCell + Admin nav counts 7→8 with Label clarification). All 8 verifiers green: 58/15/36/22/14/27/22 + 21/21 (07) + 19/19 (08).
| 09 Notifications + SMTP                                                              | `Phase_09_Notifications_SMTP.md`  | ✅ **Done (19/19) — 2026-07-27.** `ImpactSubmissionController::notifyReportSubmitted()` hook (post-`store()` for `type=report` only) → queries `NotificationSetting::where('action', 'WEEKLY_REPORT_SUBMITTED')->where('enabled', true)` → `Mail::raw()` per-rule wrapped in `try { … } catch (\Exception $e)` with `Log::warning` per recipient (never aborts the loop); `.env.example` documents the prod-switch protocol (flip `MAIL_MAILER=smtp` + fill MAIL_HOST/PORT/USERNAME/PASSWORD); dev default kept on `MAIL_MAILER=log` so mails route to `storage/logs/laravel.log` without an SMTP server. `NotificationSettingsController` admin-only CRUD (index / store via `updateOrCreate` composite upsert on `[action, recipient_email]` with `in:WEEKLY_REPORT_SUBMITTED,GUEST_ASSIGNED` validation / destroy). `Pages/Notifications/Settings.tsx` form (rule-action select with both options + rule-email + rule-submit button) + Active Rules table with `router.delete('/notification-settings/${id}')` Remove. 3 routes: `GET/POST /notification-settings` + `DELETE /notification-settings/{id}`. Admin nav "Notifications" entry. `scripts/verify_phase09_run.php` — 19 source-pattern sub-assertions (`[1]-19`: migration shape, model fillable+casts, controller admin-gating+upsert, routes, nav, Settings.tsx UX, `ImpactSubmissionController` wiring, graceful skip + fail, `.env MAIL_*` keys, `config/mail.php smtp` block); `[14]` is a bulletproof `str_contains` triple; `[15]` adds structural `strpos` ordering (`try {` before `Mail::raw(` before `catch (\Exception $e)`) per the per-rule graceful-fail contract. **Phase 09b** follow-up shipped (see row 09b below) — all 3 originally-deferred items addressed. Phase 02-08 + 06d.0/06d.1/06d.2 regression net unchanged. |
| 09b Notifications polish                                                              | `Phase_09_Notifications_SMTP.md`  | ✅ **Done (15/15) — 2026-07-27.** Spec § *Test email 7* + *Settings page 5* resolved. (a) GuestController has `private function sendGuestAssignedNotification(Guest $guest): void` mirroring Phase 09 try/catch+log shape; `store()` fires it after `Guest::create(...)` (initial assignment); `update()` captures `$beforeCellId = $guest->nearest_impact_cell_id` BEFORE write + conditionally calls helper on change via `if (($beforeCellId ?? '') !== ($guest->nearest_impact_cell_id ?? ''))` gate (no fire on no-op updates). Helper queries `NotificationSetting::where('action','GUEST_ASSIGNED')->where('enabled', true)->get()` + skips-with-`Log::info('GUEST_ASSIGNED notification skipped: …')` when no rules + per-recipient `Mail::raw()` wrapped in `try { … } catch (\Exception $e) { Log::warning('Failed to send GUEST_ASSIGNED email to …'); }` (Phase 09 graceful-fail contract preserved). (b) New `POST /notification-settings/test-email` (route `notification-settings.test-email`) → `NotificationSettingsController::testEmail(Request): JsonResponse` admin-gated + wraps `Mail::raw()` in try/catch + returns `{sent: bool, message: string}` JSON (200 on success, 500 on failure with `Log::warning` per recipient). (c) `NotificationSettingsController::index()` payload now includes `mailConfigured: bool` from new private `isMailConfigured()` (true iff `config('mail.default') === 'smtp'` AND all 4 required `mail.host`/`mail.port`/`mail.username`/`mail.password` keys are populated); `Pages/Notifications/Settings.tsx` renders `<span data-testid="mail-config-badge">` with `✓ SMTP configured` (emerald) OR `⚠ Mail: log (dev)` (amber) + per-rule `Send test email` button (per-rule `data-testid="send-test-${id}"` + `data-testid="test-result-${id}"` success/fail span) that POSTs to `/notification-settings/test-email` with `recipient_email: s.recipient_email` via `fetch()` + CSRF-token-from-meta. `scripts/verify_phase09b_run.php` — 15 source-pattern sub-assertions (rewritten in a polish round with str_contains-only assertions to absorb whitespace tolerance, no complex regex). (d) **Polish round discovery (2026-07-27)**: handler `NotificationSettingsController::testEmail(Request): JsonResponse` already existed + `Settings.tsx` already rendered the Send test email button, BUT `routes/web.php` did NOT register the `POST /notification-settings/test-email` route — this would have silently 404'd in production. Polish round added the missing `Route::post('/notification-settings/test-email', [\App\Http\Controllers\NotificationSettingsController::class, 'testEmail'])->name('notification-settings.test-email');` line inside the existing `auth` middleware group, restoring full 4/4 notification-settings route surface. **HANDOFF §0 verifier-strictness lesson**: the [12] assertion checking `Route::post` + `/notification-settings/test-email` + `'testEmail'` + `'notification-settings.test-email'` (all 4 substrings required) would have caught this gap at original Phase 09b ship time if the verifier had been run. Future polish rounds must NOT soften multi-substring assertions on named-route endpoints.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         || 10 CSV import/export                                                                  | `Phase_10_CSV_Import_Export.md`   | ✅ **Done (19/19) — 2026-07-27.** `CsvImportController::index()` admin-gated via `abort_unless($request->user()?->activeRole() === 'Administrator', 403)` rendering the Inertia `Csv/Import` page; `CsvImportController::import()` (admin-gated + validates `['csv' => 'required|file|mimes:csv,txt', 'template' => 'nullable|in:officer,team,impact']` + parses via `array_map('str_getcsv', file($file->getRealPath()))` per spec CSV-parsing contract + 5-alias `phone` map `['phone','phone number','mobile','tel','telephone']` + duplicate-detection by phone via `Guest::where('phone', $phone)->exists()` + skips rows with empty phone + reports each skipped row in `$skipDetails` + responds with `{created, skipped, errors}` JSON shape); `CsvExportController::export()` (gated via `RoleHelper::groupOf($role)` for Admin OR `followUpOfficer`-group OR `followUpTeam`-group + private `columnsForRole('Administrator')` returns 14 columns covering all 3 group-owned column-sets including `nearest_impact_cell_id` + `follow_officer_id` + `follow_up_status` + `follow_up_contacts` + fallback returns 8-column officer/team subset `[guest_name, phone, email, event, source, contacted_status, visited, created_at]` EXCLUDING the group-owned columns); `routes/web.php` registers 3 csv routes — `GET /csv/import` (`csv.import` → CsvImportController@index), `POST /csv/import` (`csv.import.upload` → CsvImportController@import), `GET /csv/export` (`csv.export` → CsvExportController@export); `resources/js/Pages/Csv/Import.tsx` is the bulk-import UI (drag-and-drop dropzone with `data-testid="csv-drop-zone"` + hidden `data-testid="csv-file-input"` ref + `data-testid="csv-upload-button"` POST button → `fetch('/csv/import', { method: 'POST', body: formData })` + result panel `data-testid="card-csv-result"` rendering `StatusPill` for `Created:` + `Skipped:` + `Errors:` + collapsible `<details>` accordion listing each skip reason); `scripts/verify_phase10_run.php` — 19 source-pattern sub-assertions in the established Phase 09-shape pattern (`[1]` self-syntax; `[2]` spec-doc shape; `[3]-10` CsvImportController class-shape + index admin-gate + import admin-gate + validation + `str_getcsv` parser + 5-alias `phone` map + duplicate-by-phone + `{created,skipped,errors}` JSON shape + empty-phone skip; `[11]-14` CsvExportController class-shape + 3-group RoleHelper gate + Admin columns-presence-of-group-owned + fallback-excludes-group-owned; `[15]` 3 csv routes registered; `[16]-19` Import.tsx default-function-export + 3 upload testids + AuthenticatedLayout/useRef/fetch POST + card-csv-import + card-csv-result with StatusPill). **Deferred to Phase 10b** (no regression risk; non-blocking): (a) `GUEST_CSV_COLUMNS(role)` helper (spec § in `access.ts`) — currently inlined inside `CsvExportController::columnsForRole`, doesn't read from `RoleHelper::canEditField`; (b) per-template column header sync (spec §3 templates) — `template` validator accepts `officer,team,impact` but the alias map is single-set; (c) `auditBatch(req, 'GUESTS_IMPORTED', $created)` (spec §7) — import currently doesn't write to activity log. Phase 02-09 + 06d.0/06d.1/06d.2 + 06c regression net unchanged. **Phase 10b (15/15) shipped 2026-07-27** — all 3 deferred items resolved (see §1 Phase 10b row): `app/Support\CsvColumns` helper extracted + `aliasesForTemplate` per-template sync (officer/team/impact/default) wired into `CsvImportController` + `activity('csv-import')->log('GUESTS_IMPORTED')` Spatie-Activitylog audit call wired into the import loop. **Verifier fixes shipped mid-10b**: scripts/verify_phase10_run.php [13] re-pointed to read from $csvColumnsSrc + added `! str_contains('private function columnsForRole')` inverse-check; [14] positive exact-list match (replacing brittle global-negative regex); 1-character `\$csvColumnsPath`→`$csvColumnsPath` backslash fix; duplicate-string-tail collapse in check(13) expected-message. Phase 02-09 + 06b/06c + 06d.0/06d.1/06d.2 + 07 + 08 regression net unchanged at 58/15/36/22/14/27/21/22/19/15/15/19/15/19/19 — **Phase 10b joins as the 3rd sub-phase bundle** alongside 06b+06c + 09b. |
| 10b CSV polishing                                                             | `app/Support\CsvColumns.php` + `scripts/verify_phase10b_run.php` | **Done (15/15)** — 2026-07-27. **(a)** `app/Support\CsvColumns.php` (new SOOT helper) — `forRole(?string $role): array` (14-col Admin / 8-col fallback subset) + `aliasesForTemplate(?string $template): array` (per-template enrichment: `officer` → `contacted_status` / `visited`, `team` → `follow_up_status`, `impact` → `impact_status` / `nearest_impact_cell_id`, default → base-only). **(b)** `CsvExportController` refactored — imports `App\Support\CsvColumns` + calls `CsvColumns::forRole($role)` (private `columnsForRole` method removed). **(c)** `CsvImportController` refactored — imports `App\Support\CsvColumns` + uses `$aliasMap = CsvColumns::aliasesForTemplate($request->input('template'))` (single-set alias map retired) + adds fluent Spatie-Activitylog auditBatch call: `activity('csv-import')->causedBy($request->user())->withProperties(['created'=>$created, 'skipped'=>$skipped, 'template'=>$request->string('template')->toString()])->log('GUESTS_IMPORTED');` after the import loop. **(d)** `resources/js/Pages/Csv/Import.tsx` updated — added `<select data-testid="csv-template-select">` (officer/team/impact/default options) wired to `useState<string>('')` + `formData.append('template', template)` in the upload submit. **(e)** `scripts/verify_phase10b_run.php` (new) — 15 source-pattern sub-assertions (`[1]` self-syntax; `[2]-4` CsvColumns class-shape + `forRole` + `aliasesForTemplate` methods; `[5]-9` `forRole` Admin 14-col branch + non-Admin 8-col fallback + `aliasesForTemplate` officer/team/impact/default branches; `[10]-11` CsvExportController calls `CsvColumns::forRole` + private `columnsForRole` removed; `[12]-13` CsvImportController calls `CsvColumns::aliasesForTemplate` + `activity('csv-import')->log('GUESTS_IMPORTED')` chain; `[14]` `routes/web.php` still has 3 csv routes intact; `[15]` `Import.tsx` has `<select>` + `formData.append('template', ...)`). **No runtime regression** — Phase 02-10 + 06b/06c + 06d.0/06d.1/06d.2 + 07 + 08 + 09 + 09b (known 9/6 brittle-verifier state) + 11 + 12 sub-phase regression net unchanged. || 11 Reports + Audit                                                                    | `Phase_11_Reports_And_Audit.md`   | ✅ **Done (19/19) — 2026-07-27.** `ReportsController::index()` 5-role gated (`Administrator` + `Supervisor` + `Impact_Cell_Admin` + `Impact_Cell_Report` + `Follow_UP_Admin` via `in_array(..., true)` with 403) + month scoped via `whereYear('created_at', ...)` + `whereMonth('created_at', ...)` (default `$request->get('month', now()->format('Y-m'))`); 5 computed KPIs (`pendingContacts` via `whereNull(contacted_status) OR empty OR orWhereIn(['No','Not Contacted'])` → `count()`; `totalCalls` via `whereNotNull + != ''`; `visited` via `where('visited', true)`; `pendingVisit` via `AvailableForVisit + visited=false`; `responseRate = round(($visited/$totalCalls) * 100, 1)` with 0-fallback when `$totalCalls==0`); 4 group-by breakdowns (`byStatus` + `byEvent` + `byFollowUp` with `COALESCE(NULLIF(follow_up_status, ''), 'NOT CONTACTED')` + `monthly` via `DATE_FORMAT(created_at, '%Y-%m')` selectRaw → groupBy → orderBy('ym')); Inertia `Reports/Index` payload `{kpis, byStatus, byEvent, byFollowUp, monthly, month}`. `AuditLogController::index()` admin-gated (`activeRole === Administrator`) + `Activity::with('causer:id,name')->latest()`; 3 optional filters (`actor` → `where('causer_id', $actor)`; `entity` → `where('subject_type', 'App\Models\' . ucfirst($entityType))`; `entity_id` → `where('subject_id', $entityId)`); `->limit(500)` + Inertia `Audit/Index` payload `entries.map(fn (Activity $a) => ['id', 'description', 'actor' (=> $a->causer?->name ?? 'System'), 'subjectType' (=> class_basename($a->subject_type ?? '')), 'subjectId', 'properties', 'createdAt' (=> $a->created_at?->toIso8601String())])`. `routes/web.php` registers 2 routes (`GET /reports` = `reports.index` → ReportsController@index, `GET /audit` = `audit.index` → AuditLogController@index). `resources/js/Pages/Reports/Index.tsx` — 5-KPICard grid (5 captions: Pending Contacts / Total Calls / Visited / Pending Visit / Response Rate) + month filter `<input type="month">` onChange → `router.get('/reports', { month }, { preserveState: true })` + Export CSV `<Link href={route('csv.export')}>` + 4 recharts chart cards (`By Contact Status` BarChart + `By Follow Up Status` BarChart + `By Event` BarChart + `Monthly Trend` AreaChart) using `ResponsiveContainer` + `CartesianGrid` + `Bar`/`Area` + per-dataKey Tooltip. `resources/js/Pages/Audit/Index.tsx` — `useState<Entry|null>(null)` selected-card pattern + `data-testid="card-audit"` shell + `data-testid="audit-table"` table (Date / Actor / Description / Subject / Details-button) + `data-testid="card-audit-details"` side-panel rendering `JSON.stringify(selected.properties, null, 2)`. `scripts/verify_phase11_run.php` — 19 source-pattern sub-assertions in the established Phase 09/10 shape (`[1]` self-syntax; `[2]` spec-doc shape; `[3]-10` ReportsController class-shape + 5-role gate + month-scope default + 5 KPI keys + pendingContacts-whereNull/empty/in + responseRate round-100-1 + Inertia render + Reports/Index 'month' key + 4 group-by breakdowns; `[11]-15` AuditLogController class-shape + admin-gate + Activity::with(causer:id,name)->latest + 3 optional filters (causer_id/subject_type-via-ucfirst/subject_id) + Inertia render + limit(500) + 7-key entries-map; `[16]` 2 routes registered via single-line `[^;]*` co-proximity; `[17]-19` Reports.tsx 5 KPICard captions + recharts wrapper + Audit.tsx testids + useState<Entry|null> + JSON.stringify side-panel + month-input router.get + csv.export href). **Resolved via Phase 11b (15/15 shipped 2026-07-27)** — see Phase 11b row immediately below. Originally-deferred items all shipped: (a) `/api/reports/audit` JSON endpoint + (b) `By Join When` recharts donut + (c) Officer Performance top-10 table + (d) Audit side-panel before/after DiffViewer. Phase 02-10 + 06b/06c + 06d.0/06d.1/06d.2regression net unchanged. |
| 11b Reports + Audit polish | `Phase_11_Reports_And_Audit.md` | ✅ **Done (15/15) — 2026-07-27.** Phase 11 deferred items (a)-(d) all resolved. **(a)** NEW `GET /api/reports/audit` (route `api.reports.audit`) → `AuditLogController::apiIndex(Request): JsonResponse` returns `response()->json()` of the same `Activity::with('causer:id,name')->latest()->limit(500)` + 3-filter (causer_id / subject_type via `ucfirst` / subject_id) shape as `index()` so curl/clients can consume the same audit list. **(b)** NEW `By Join When` recharts donut (`<PieChart><Pie><Cell>` inside `<ChartCard testId="card-joinwhen-chart">`); controller `Guest::selectRaw("join_when, COUNT(*) as cnt")->groupBy('join_when')->orderByDesc('cnt')` returns new payload key `'byJoinWhen'`. **(c)** NEW Officer Performance top-10 table — controller `DB::table('guests')->join('users', 'guests.follow_officer_id', '=', 'users.id')->selectRaw('users.id, users.name, COUNT(guests.id) as total, COALESCE(SUM(guests.visited), 0) as visited')->whereNotNull('guests.follow_officer_id')` + **month-scope** `whereYear('guests.created_at', substr($month, 0, 4))->whereMonth('guests.created_at', substr($month, 5, 2))` (UX parity with all other month-filtered charts — prevents the inconsistency where month-switch updates all charts EXCEPT the top-10) + `->groupBy('users.id', 'users.name')->orderByDesc('total')->limit(10)` + `conversion_rate = round(($row->visited / $row->total) * 100, 1)`; new payload key `'officerPerformance'`. **(d)** NEW `DiffViewer` component in `Audit/Index.tsx` (replaces prior `JSON.stringify(selected.properties, null, 2)` JSON dump) — reads `properties.old` + `properties.attributes` Spatie Activitylog shape; renders per-key `<del class="text-red-600 line-through no-underline">` for old values + `<ins class="text-emerald-600 no-underline">` for new values; defensive `JSON.stringify(properties, null, 2)` fallback for unknown shapes; wrapped in `<div data-testid="audit-diff-viewer">`. `scripts/verify_phase11b_run.php` — 15 sub-assertions in the established Phase 09b str_contains-only verifier shape (`[1]` self-syntax; `[2]-4` AuditLogController `apiIndex` `JsonResponse` + `response()->json` + `->limit(500)` + 3 wheres on `causer_id`/`subject_type`/`subject_id`; `[5]` routes/web.php `Route::get` + `/api/reports/audit` + `'apiIndex'` + `'api.reports.audit'`; `[6]-9` ReportsController `byJoinWhen` guest-aggregation + `officerPerformance` users-join + `conversion_rate` round math + `orderByDesc('total')` + `->limit(10)`; `[10]-12` Reports/Index.tsx recharts `PieChart/Pie/Cell` imports + `<PieChart>` rendering for By Join When + Officer Performance top-10 table rendering with `conversion_rate` column + `data-testid="card-officer-performance"`; `[13]-15` Audit/Index.tsx `DiffViewer` Spatie `properties.old`/`properties.attributes` shape + `<del>`/`<ins>` red/green semantics + `data-testid="audit-diff-viewer"` wrapper). **Verifier-hardening shipped mid-11b** (Phase 09b-style polish-pattern reused): (i) Phase 11b `[11]` needle swap — `data-testid="card-joinwhen-chart"` → `testId="card-joinwhen-chart"` (ChartCard wrapper receives `testId` PROP, not `data-testid` ATTRIBUTE — source needles must match the prop string passed by callers, not the rendered DOM attribute, because this verifier scans SOURCE text) + 8-line docblock anchor comment preventing re-introduction; (ii) Phase 11 verifier `[18]` OR-widening tightened — dropped the transitional `(str_contains(src, \"JSON.stringify(selected.properties\") || str_contains(src, \"data-testid=\"audit-diff-viewer\"\"))` disjunct now that the old `selected.properties` side-panel is fully removed from `Audit/Index.tsx`, keeping ONLY the strict `data-testid=\"audit-diff-viewer\"` needle — eliminates verifier-permissiveness accumulation; (iii) Officer Performance month-scope insertion between `->whereNotNull('guests.follow_officer_id')` and `->groupBy(...)` for UX parity with the other 5 month-filtered payloads. Phase 02-10 + 06b/06c + 06d.0/06d.1/06d.2 regression net unchanged at **+ Phase 11b (15/15) — 262/262 assertions across 12 primary phases + 3 sub-phase bundles (06b+06c + 09b + 11b)**.
| 12 Deployment | `Phase_12_Deployment.md`          | ✅ **Done (19/19) — 2026-07-27.** Production deploy to `https://app.summitdata.one/` (Hostinger) per `Implementation/Phase_12_Deployment.md`. **No runtime code changes** in this phase — Phase 12 bakes the v2-build deploy story into a 7-section deploy checklist (`Pre-deploy verification` → `Build locally` → `Upload to Hostinger` → `Restart Passenger` → `.htaccess completeness` → `Verify URL` → `Smoke tests`) + `Rollback` procedure (`mv dist dist.broken` + `cp -r dist.previous dist` + `touch tmp/restart.txt`) + `Final go/no-go checklist`. Critical Hostinger artifacts documented: `/home/u188660189/domains/app.summitdata.one/public_html/.builds/last-source/dist/{client,server}/` upload path; SSH `export PATH=/opt/alt/alt-nodejs22/root/bin:$PATH` to access Node-22 alt-runtime; `PassengerAppType node` + `PassengerStartupFile server/server.js` + `PassengerAppRoot` + `PassengerRestartDir` .htaccess directives (load-bearing); 3-user-group live verify logins (`sbcAdmin` admin / `FollowUpOfficer` officer / `Impact_Leaders` cell-leader); no-500 verify endpoints `/api/health` + `/login` + `/dashboard` for all 3 groups; Audit log per-write coverage (`guest create` + `guest update` + `member submission` + `weekly report submitted`). `scripts/verify_phase12_run.php` — 19 source-pattern sub-assertions (`[1]` self-syntax; `[2]-16` spec-doc coverage of the 7-section deploy checklist + .htaccess directives + 3-user-group verifications + Audit log per-write coverage + Rollback + Final go/no-go; `[17]-19` HANDOFF state — Phase 12 row ✅ Done + last-verified-green inclusion + `Next: Phase 12` retired). **Project complete** — 7 architecture docs + 12 phase docs = 19 docs total per Phase 12 spec closing count. Phase 02-11 + 06b/06c + 06d.0/06d.1/06d.2 regression net unchanged at 58/15/36/22/14/27/21/22/19/15/15/19/15/19/19/19 across Phase 02/03/04/05/06/06b+06c/07/06d.0/08/06d.1/06d.2/09/09b/10/11/12. **No follow-on shipping phase remaining**; Phase 12b can tackle the rolled-over deferred items from Phase 09b (`GUEST_ASSIGNED_MAIL` caller + Test-email button + smtp-badge) + 10b (`GUEST_CSV_COLUMNS` helper + per-template column sync + auditBatch) + 11b (`/api/reports/audit` + Join Donut + Officer Perf + Audit diff viewer) in a separate polish round. |
| 12b **v2 Deploy spec + script + cheatsheet + 2 polish edits** | `Implementation/Phase_12_Deployment.md` + `scripts/deploy_to_hostinger.sh` + `scripts/HOSTINGER_DEPLOY_CHEATSHEET.md` | ✅ **Shipped (2026-07-27) — same 19/19 verification assertion count retained after spec rewrite.** Spec fully rewritten from v1 Node narrative (`dist/client` + `dist/server` + `PassengerAppType node` + `/opt/alt/alt-nodejs22/root/bin` + `tmp/restart.txt` + `PassengerRestartDir`) **TO v2 Laravel 12 + Vite** (`composer install --optimize-autoloader --no-dev` + `npm run build` → Vite emits `public/build/` + `php artisan migrate --force` + Hostinger DocumentRoot → `laravel/public/` + `.env` prod creds + `chmod -R 775 storage bootstrap/cache` + `php artisan config:cache route:cache view:cache storage:link`). NEW `scripts/deploy_to_hostinger.sh` (~145 lines bash w/ `set -euo pipefail`): **10 functions** (preflight + backup + upload + composer_install + artisan_round + fix_permissions + verify_htaccess + verify_health + verify_3group_smoke + rollback); 3 entrypoint dispatch (`deploy` default \| `rollback` \| `health`); v1 Node leak tokens now **0** (`dist/client` ×0, `dist/server` ×0, `PassengerAppType node` ×0, `alt-nodejs22` ×0). NEW `scripts/HOSTINGER_DEPLOY_CHEATSHEET.md`: 7-section one-page deploy cheat sheet (preflight + SSH connect + composer+artisan + chmod+chown + health verify + 3-user-group logins + rollback), readable in ~90s, paste-into-terminal oriented. **2 polish edits applied** (Round-2 code-reviewer follow-ups): (a) `--exclude '.previous'` added to rsync `--delete` (defensive against accidental local `.previous/` upload); (b) `ssh ... "mkdir -p $APP_PATH"` step inserted before rsync+scp (fixes first-deploy scp "no such file" race when `$APP_PATH` doesn't yet exist on host). **v2-deploy preflight ran locally** (Windows dev box): `composer install --no-dev` ✅ (Laravel 12.64.0 + 26 prod packages); `php artisan storage:link` ✅; `php artisan route:cache` + `config:cache` + `view:cache` ✅ (4 cache files in `bootstrap/cache/`); `npm install` ❌ blocked by Vite-7 peer-dep conflict (`vite@7.3.6` peerRequires `@types/node@^20.19.0 \|\| >=22.12.0` vs project-pinned `^18.13.0`) → **`public/build/` empty → `npm run build` cannot produce prod assets until peer-dep is bumped** (this is the real blocker for production push). `scripts/verify_phase12_run.php` re-anchored 19/19 GREEN after spec v2 pivot: `[2]-16` re-assert Laravel tokens (`composer install --no-dev` + `public/build` + `migrate --force` + `DocumentRoot → public/` + `chmod -R 775 storage bootstrap/cache` + 3-user-group + Audit-log 4-write + Rollback + Final go/no-go); `[17]-19` HANDOFF state unchanged (Phase 12 row ✅ Done + last-verified-green `Phase 12 (19/19)` inclusion + `Next: Phase 12` retired → `Project feature-complete` marker). **Master runner** `scripts/verify_all_phases_run.php`: 11/11 PRIMARY phases syntax-clean + 261/261 assertions GREEN (post re-base). **Round-5 polish (2026-07-27)**: **`@types/node` peer-dep fix shipped** — `package.json` devDependencies. **`@types/node`** bumped from `^18.13.0` → `^20.19.0` (proper version-range fix per Vite 7 peerOptional `^20.19.0 || >=22.12.0`; NOT `--legacy-peer-deps` per user directive). `npm install` now clean — `vite@7.3.6` resolves without peer-dep error. Master + Phase 12 verifier unchanged (GREEN). **RESOLVED (2026-07-27)** — `resources/js/Components/AdminSidebar.tsx` had pre-existing TS syntax errors from a prior LLM token-stream artifact (`</function</thinkingcontent<function_calls>` + `<invoke name="write_file">` + `<parameter name="content">` tags leaked into the file body + duplicate broken `type NavSpec = {` declaration with stray `;`). Surgical cleanup: deleted the corrupted 14-line prefix via str_replace; prepended the missing legitimate `import AdminSidebarNavItem from '@/Components/AdminSidebarNavItem';` (which the cleanup round had accidentally over-deleted); fixed a SEPARATE latent bug in `resources/js/Layouts/AdminDashboardLayout.tsx` where `searchIndex` was in the props type but not destructured in the function signature (`TS2304: Cannot find name 'searchIndex'`) — added `searchIndex,` to the destructure pattern. **Result**: `tsc --noEmit -p tsconfig.json` returns 0 errors; `npm run build` succeeds (rc=0); `public/build/manifest.json` populated + `public/build/assets/` contains 1 CSS file + 51 JS files. **Production-push blocker fully resolved** — Hostinger deploy is now a single SSH + run-script operation. **LATENT BUG noted for Phase 13 polish (non-blocking)**: `AdminSidebar.tsx` NAV_ITEMS lines 8 + 13 both use `routeName: 'notification-settings.index'` for "Notifications" AND "Settings" rows; when route is active, both nav items highlight. Fix plan: Notifications → `'notifications.index'` (or `'admin.notifications.index'`) + Settings → `'admin.settings.index'` (or `'profile.edit'` as user-settings fallback). **Phase 02-11 + 06b/06c + 06d.0/06d.1/06d.2 regression net unchanged** at 58/15/36/22/14/27/21/22/19/15/15/19/15/19/19 (13 primary phases + 2 sub-phase bundles (06b+06c + 09b) = 58/58 + 15/15 + 36/36 + 22/22 + 14/14 + 27/27 + 21/21 + 22/22 + 19/19 + 15/15 + 15/15 + 19/19 + 15/15 + 19/19 + 19/19 across Phase 02/03/04/05/06/06b+06c/07/06d.0/08/06d.1/06d.2/09/09b/10/11). **No follow-on shipping phase remaining**. **Real production-push prerequisite remaining**: bump `@types/node` from `^18.13.0` → `^20.19.0` to satisfy Vite 7 peer range — once done, `npm install && npm run build` populates `public/build/` → Hostinger deploy becomes a single SSH + run-script operation. |

**Last verified green: Phase 02 (58/58) + Phase 03 (15/15) + Phase 04 (36/36) + Phase 05 (22/22) + Phase 06 (14/14) + Phase 06b+06c (27/27) + Phase 07 (21/21) + Phase 06d.0 (22/22) + Phase 08 (19/19) + Phase 06d.1 (15/15) + Phase 06d.2 (15/15) + Phase 09 (19/19) + Phase 09b (15/15) + Phase 10 (19/19) + Phase 10b (15/15) + Phase 11 (19/19) + Phase 11b (15/15) + Phase 12 (19/19) + Phase 13 (13/13), 2026-07-27** — Phase 06d.0 (Admin Dashboard polish prelude) shipped today with all 22 sub-assertions green + zero regression on Phase 02-07. **Phase 06d.1 (Admin Overview Analytics — recharts AreaChart + DateRangeFilter) shipped today with all 15 sub-assertions green** — eager HeadlessUI Popover DateRangeFilter (4 presets + Custom popover), lazy recharts AreaChart (4 cumulative metrics), controller `parseRange(buildChartSeries(seriesForRange())` helpers + 5 new payload keys. **Phase 06d.2 (System Overview + Recent Activity + Recent Registrations + Footer + wired GlobalSearch + wired LanguageSwitcher) shipped today with all 15 sub-assertions green** — 4 progress-bar cards (DB size try/caught for SQLite compat + Storage + Active Users + System Health), 6 colored-icon Recent Activity tiles (Guests/Cells/Reports/Notifications/Audit/Users), mixed-source Recent Registrations feed (3 guests + 3 users + 3 submissions with avatar initials + RelativeTime helper), wired HeadlessUI Combobox GlobalSearch + Listbox LanguageSwitcher in AdminDashboardLayout, branded FooterCard in footer slot. `scripts/verify_phase06d0_run.php` [2] updated to assert the post-06d.2 architectural contract (component imports + usage, not literal placeholder testids that relocated into the components). Submissions-stub back-button text still aligns with the 4 placeholder stubs ('← Back to Dashboard'). Phase 08 (Leadership Board UI) still paused mid-ship — controller + show + index + initialData prop + Index.tsx written, but routes + nav + HANDOFF still TODO. **Resuming Phase 08 is the next ship** (the last of the four sub-phases in the post-06 polish ensemble). (the only new assertion ADDED in the post-Phase-05 snake_case-matrix commit is the 18-row `[1b]` shape guard in `verify_phase02_run.php`. Phase 03 + Phase 04 grew their counts in earlier refactors that split numbered cases into multiple sub-assertions `[6]/[7]/[8]/[12]/[13]` — not from the snake_case commit). After the Phase 05c follow-ups (Controller trait fix + edit form + FE/BE column-policy mirror), all 5 verifiers re-ran clean at 58/15/36/22/14 — no regression, no new assertion added. **Phase 06b foundation (Tokens + Chrome + 3 primitives) shipped today with `scripts/verify_phase06b_run.php` — all 23 sub-assertions pass on the first clean run**; Phase 02-06 verifiers re-ran with no regression (58/15/36/22/14 unchanged).

- `scripts/verify_phase02_run.php` — source of truth for Phase 02 — runs the auth + RBAC + RoleHelper + active-role checks.
- `scripts/verify_phase03_run.php` — source of truth for Phase 03 — runs the impact_cell hierarchy + seeder idempotency checks.
- `scripts/verify_phase04_run.php` — source of truth for Phase 04 — runs the guest FK + Form Request stripping + Resource masking + Policy + ImpactCellPolicy checks.
- `scripts/verify_phase05_run.php` — source of truth for Phase 05 — runs the officer KPI math + role-aware top-bar nav + reassign-permission + seeder idempotency checks.
- `scripts/verify_phase06_run.php` — source of truth for Phase 06 — runs the team user seeding + policy assertions + DashboardController team variant + KPI presence.
- If any prints anything other than `N pass / 0 fail`, fix the regression before continuing.

---

| 13 Admin sidebar routeName collision fix | — (resolves §1 Phase 12b footer latent bug) | ✅ **Shipped (13/13) — 2026-07-27.** Bug: `AdminSidebar.tsx` NAV_ITEMS lines 8 + 13 both used `routeName: 'notification-settings.index'` for "Notifications" + "Settings" (both highlighted when /notification-settings active). **Fix (paired href + routeName)**: Settings `routeName: 'notification-settings.index'` → `'profile.edit'` (user-settings fallback — `'admin.settings.index'` does not exist as a route, so `profile.edit` was the only existing-route option) + matched `href: '#settings'` → `href: route('profile.edit')` per code-reviewer bullet #1 (so clicking Settings navigates to /profile/edit; previously clicking did nothing). Notifications row unchanged. **Active-state contract preserved** in both `AdminSidebar.tsx` + `AuthenticatedLayout.tsx` (`currentRoute === item.routeName`). `scripts/verify_phase13_run.php` — 13 sub-assertions (`[1]` self-syntax; `[2]-3` NAV_ITEMS shape + 13 labels; `[4]` CRITICAL collision guard `substr_count('routeName: \'notification-settings.index\'') === 1` — was 2 before fix; `[5]` Notifications keeps canonical; `[6]` paired href+routeName fix on Settings (positive `href: route(\'profile.edit\')` + `routeName: \'profile.edit\'` + negative `'#settings'` + `'notification-settings.index'`); `[7]-10` `routes/web.php` has both `profile.edit` + `notification-settings.index`, does NOT register `admin.notifications.index` + `admin.settings.index`; `[11]-12` active-state contract preserved; `[13]` HANDOFF §1 Phase 13 row present). Phase 02-12 + 06b+06c + 06d.x + 09b + 10b + 11b regression net unchanged at**+ Phase 13 (13/13) — 275/275 assertions across 12 primary + 4 sub-phase bundles (06b+06c + 09b + 10b + 11b + 13)**. |
| 13b Phase 13 follow-on bundle (signup trim + cell-setup toggle + flatten-all-cells + Users/Index CRUD promotion) | `database/migrations/2026_07_31_120000_flatten_all_impact_cells_to_primary.php` + `app/Support/RoleHelper.php` + `resources/js/Pages/Auth/Register.tsx` + `resources/js/Pages/Admin/Users/Index.tsx` + `scripts/verify_phase13b_run.php` | ✅ **Shipped (18/18) — 2026-07-31.** **(A) Signup role trim** — `RoleHelper::SIGNUP_VISIBLE_ROLES` reduced from a 5-role inclusive array (FollowUpOfficer + Follow_UP_Admin + Follow_UP + Follow_UP_View_Only + Impact_Leaders + Impact_Zonal_Coordinator) DOWN TO `[Impact_Leaders, Impact_Zonal_Coordinator]` only (Administrator + all 4 FollowUp_* family roles removed from public-facing signup; only the 2 Impact_Cell-group-leadership roles remain, exactly the 2 the registration form's leadership-team panel was designed to anchor). `RegisterInertiaRequest::rules()` `Rule::in(RoleHelper::SIGNUP_VISIBLE_ROLES)` now rejects any `roles[]` containing `FollowUpOfficer` / `Follow_UP_Admin` / `Follow_UP` / `Follow_UP_View_Only` on POST automatically — defense-in-depth so a future client-side re-introduction cannot smuggle a FollowUp_* role past the validator. **(B) Conditional cell-setup panel** — `resources/js/Pages/Auth/Register.tsx`: the leadership-team fields (leader_name / leader_phone / assistant_name / assistant_phone / welfare_name / welfare_phone) + cell-picker dropdown + cell-detail panel now wrapped in a SINGLE conditional `<section data-testid="register-cell-setup-block">` that ONLY renders when `data.roles.includes('Impact_Leaders')` (computed via a new `const requiresCell = data.roles.includes('Impact_Leaders')` boolean). When `requiresCell` is false (i.e. the user picked only `Impact_Zonal_Coordinator`), instead a dashed-border `<div data-testid="register-cell-setup-empty">` placeholder renders explaining (a) the panel becomes visible as soon as the user ticks `Impact_Leaders` and (b) `Impact_Zonal_Coordinator`-only signups intentionally skip cell selection — admin assigns the zone in a separate admin flow. CRITICAL: the form's 12 leadership-team keys (the 6 cell-team fields + 6 cell-setup keys) are STILL POSTED to the server even when the conditional panel is collapsed — the server-side `requiredIf(roles, Impact_Leaders)` + `Rule::in($visibleRoles)` gates validate server-side, NOT client-side (server-side is the source of truth). **(C) Flatten-all-cells-to-primary migration** — `database/migrations/2026_07_31_120000_flatten_all_impact_cells_to_primary.php` (NEW idempotent migration) bulk UPDATEs every row of `impact_cells` to `is_primary = true` + `parent_cell_id = NULL` (DB state after migration: 69 primary cells, 0 sub-cells, 0 rows with non-null parent — confirmed via tinker `ImpactCell::where('is_primary', true)->count() === 69` and full-suite regression). `down()` is a deliberate no-op with rationale documented in the class docblock (data-loss on rollback — manual re-creation via parent_cell_id assignments is required, deferring automatic restoration to a future migration is preferable to silently inventing fake parent links). `ImpactCellSeeder` updated: `APO_SUB_CELL_NAMES = []` (kept as a public leaf constant so external verifier scripts still type-check when imported — `verify_phase03_run.php` [4]/[5]/[6]/[7] were rewritten to assert the new "0-sub-cells invariant" so the seeder is now idempotent across re-runs, byte-identical snapshot on a second `seed` invocation). `Auth/RegisteredUserController::create()` + `Admin/UserController::edit()` both filter the cellsList in the Inertia payload through `ImpactCell::where('is_primary', true)->ordered()` (server-side defense-in-depth — even BEFORE the migration runs, the registration form's cell-picker dropdown + admin Edit user-form's impact_cell selector both surface ONLY primary cells, never sub-cells). **(D) Admin/Users/Index promoted stub → functional CRUD page** — `resources/js/Pages/Admin/Users/Index.tsx` is no longer a "Coming soon" placeholder page. It is now a full SSR functional CRUD page (~280 lines): header band with role-aware "Administrator · Users" eyebrow + "Users management" title + total-count subtitle + inline Search `<form>` with `<input data-testid="users-search" type="search">` + `router.get(route('admin.users.index'), { search: q || undefined }, { preserveState: true, replace: true })` + `PrimaryButton data-testid="users-add-open">` opening the `Modal`-hosted `AddUserModal` (name + email + Roles-at-least-one checkbox grid `data-testid="add-user-roles-grid"` per-role with RoleBadge + active-role dropdown + password + confirmation per `Password::defaults()`) + per-row inline `<select data-testid="user-row-{id}-role-select">` that fires `router.patch(route('admin.users.update-role', u.id), { role })` + per-row `<Link data-testid="user-row-{id}-edit">` to `/admin/users/{id}/edit` + `Remove` button that fires `router.delete(route('admin.users.destroy', u.id))` (soft-delete per `User SoftDeletes` migration) + Laravel `links()` pagination `<Link href={link.url}>` rendered with active-page highlighting. `verify_phase06d.0` [9]-[13] per-kind stub-page classification UPDATED to discriminate the new CRUD contract: `functional-crud` (Users = discriminator testid `users-table` substituting for the legacy `admin-users-stub` testid which was permanently removed when the page was promoted out of placeholder status); `placeholder` (RolesPermissions / Messages / Analytics = testid + Coming soon literal); `functional-redirect` (Submissions = testid + `impact-submissions.index` canonical-list link). **`scripts/verify_phase13b_run.php`** (NEW) — 18 sub-assertions (`[1]` self-syntax; `[2]-5` RoleHelper SIGNUP_VISIBLE_ROLES reducer (asserts ONLY `Impact_Leaders` + `Impact_Zonal_Coordinator` in the array + asserts the 4 FollowUp_* family roles are NOT in the array + asserts Administrator is NOT in the array (administrator-role signup stays admin-invite-only)); `[6]-9` Auth/Register.tsx conditional-panel architecture (new `data-testid="register-cell-setup-block"` testid presence in conditional + `data-testid="register-cell-setup-empty"` placeholder testid in fallback + 12 form keys always present in the JSX + `requiresCell` boolean computation reads `data.roles.includes('Impact_Leaders')`); `[10]-13` migration file + ImpactCellSeeder `APO_SUB_CELL_NAMES` constant empty literal `[]` + Auth/RegisteredUserController `cellsList` query filters `where('is_primary', true)->ordered()` + Admin/UserController same filter); `[14]-18` Admin/Users/Index.tsx functional CRUD discriminator + 5 CRUD testids present (`users-search` / `users-add-open` / `users-add-modal` / `users-flash-error` or `users-flash-success` / `users-table` or `users-table-wrapper`) + per-row role-select testid + per-row edit testid). Phase 02-13 (parent) + 06b+06c + 06d.x + 09b + 10b + 11b + 12b regression net unchanged. **(E) User follow-up — role-card captions removed** — `resources/js/Pages/Auth/Register.tsx`: the per-role caption text below each role-checkbox card was removed for BOTH `Impact_Leaders` (the outreach-cell note) AND `Impact_Zonal_Coordinator` (the zone-assignment-by-admin note) per the user's literal instruction "let the inner card Impact_leaders and Impact_Zonal_Coordinator inner cards to cover the text". The `const description = '';` constant + its JSX `{description && (...)}` wrapper were inlined (the `<label>` is now a clean 2-child `flex flex-col gap-0.5` = checkbox+role-name row only, no phantom `ml-6` indent or text-xs caption below). Both inner role-checkbox cards now render uniformly with NO sub-caption — the inlined-design matches the existing Phase 13 role-card chrome. **`scripts/verify_phase13b_run.php` `[11]` reformulated:** was an over-specific 3-conjunct assertion (`Impact_Zonal_Coordinator` ternary-branch presence + literal `const description = ''` regex + the no-FollowUpOfficer negative); simplified to a 2-conjunct negative-prefix-only guard (asserts neither the outreach-cell literal NOR the zone-assignment literal appears anywhere in the source — sufficient because the captions are absent in both the source AND the rendered HTML/data-page payload). The Auth/Register.tsx inline comment block was rephrased to drop the literal caption substrings, avoiding the false-positive risk of the verifier seeing captions only inside adjacent documentation. All 18 / 18 verifier assertions still GREEN; full 20-verifier suite unchanged. **+ Phase 13 follow-on bundle (18/18) — 293/293 assertions across 12 primary + 5 sub-phase bundles (06b+06c + 09b + 10b + 11b + 13b).** |
| 14-18 Cumulative bundle (signup role guard + zonal cell assignment + one-credential-per-cell + scoped global search + dashboard `$user` fix) | `database/migrations/2026_07_31_150000_fix_impact_zonal_typo_in_roles_table.php` + `database/migrations/2026_07_31_160000_create_impact_cell_user_table.php` + `app/Rules/ImpactCellHasNoLiveLeader.php` + `app/Http/Controllers/DashboardController.php` + `app/Support/RoleHelper.php` + `tests/Feature/{Phase18OneCredentialPerCellTest,SignupRoleGuardTest,AdminUserCellAssignmentTest,Phase17ImpactCellEditTest,ImpactSubmissionBindingTest}.php` | ✅ **Shipped (cumulative commit `8c1688d`, 2026-08-01).** **(14) Signup role guard** — `RegisteredUserController::ensureSignupRolesSeeded()` runs on BOTH `create()` + `store()` and `abort(503)` with remediation guidance when any `RoleHelper::SIGNUP_VISIBLE_ROLES` row is missing from `roles` — a deploy-misconfig (`migrate` without `db:seed`, truncated roles table) previously surfaced as a confusing `RoleDoesNotExist` 500 deep inside `User::syncRoles()`; now it fails loudly with the exact `db:seed --class=RolesAndPermissionsSeeder` fix command. No in-memory cache — one indexed `IN` lookup, fail-loud-at-deploy intent preserved. **(15) Role-specific cell assignment** — `Impact_Zonal_Coordinator` now REQUIRES ≥1 `zonal_impact_cell_ids`: new `impact_cell_user` pivot migration (`2026_07_31_160000_create_impact_cell_user_table.php`) + `User::zonalImpactCells()` belongsToMany; `AdminUserRequest` + `Admin\UserController::assertRolesHaveCells()` enforce both leader-needs-a-cell AND zonal-needs-cells invariants on create + edit paths (FormRequest-first, controller post-rules friendly-message second); `Admin/Users/Index.tsx` + `Edit.tsx` render the multi-select zonal cell picker (`cellsList` filtered to primary cells via `cellsForAssignment()`). **(18) One-credential-per-cell invariant** — new `App\Rules\ImpactCellHasNoLiveLeader` rule + `lockForUpdate()` transaction re-checks in `RegisteredUserController::store()` + `Admin\UserController::store()`/`update()` so two simultaneous POSTs can't both claim the same Impact Cell as Impact_Leaders (race-condition belt-and-suspenders behind the FormRequest rule; update path only re-trips the lock when the cell binding is CHANGING). Covered by `tests/Feature/Phase18OneCredentialPerCellTest.php`. **(16) User-scoped global search** — `DashboardController::buildGlobalSearchIndex($user, $role, $group)` scopes each category (submissions/guests/cells/users) to the current user's own work for every role EXCEPT `Administrator`/`Impact_Cell_Admin` (system-wide baseline preserved); zonal cell IDs hoisted into ONE pivot query reused across three branches (no triple-SELECT). **Dashboard `$user` crash fix** — the `Undefined variable $user` 500 on `GET /dashboard` (introduced when `buildGlobalSearchIndex` gained its `$user` param but `adminDashboard()` + `impactCellAdminDashboard()` never defined it) fixed by `$user = $request->user();` at the top of both methods; verified in-browser: dashboard + Guests/Impact Cells/Reports/Users nav all 200, zero console errors. **Misc** — `Impact_Zonal_Cordinator` → `Impact_Zonal_Coordinator` typo migration (`2026_07_31_150000`); `ImpactSubmissionSeeder` sample history; console commands `AuditRolesCommand` + `Phase17AcoJedoUuidCommand` + `RotateSbcadminPasswordCommand`. **No new verifier script** — validated via the feature-test suite + live browser smoke; Phase 02-13b regression net unchanged. |

| 14-18 follow-on — zonal coordinator zone-scoped surface (cells-only, guests removed) | `app/Http/Controllers/DashboardController.php` + `app/Http/Controllers/GuestController.php` + `app/Policies/GuestPolicy.php` + `resources/js/Pages/Dashboard.tsx` + `resources/js/Components/AdminSidebar.tsx` | ✅ **Shipped — 2026-08-03.** User directive: remove Guests from the `Impact_Zonal_Coordinator` role; the zonal dashboard must reflect ONLY the Impact Cells Admin assigned via the `impact_cell_user` pivot (incl. the "Total Impact Cell" card count). **Changes**: (1) `DashboardController::zonalDashboard` now builds from `User::zonalImpactCellIds()` — `totalCells` = assigned count (was all 69), cells grid + recent submissions + `totalSubmissions` all scoped to assigned cells; guest KPIs (`pendingGuests`/`contactedGuests`) deleted from the payload (were system-wide). (2) `buildGlobalSearchIndex` zonal guests branch removed (search no longer surfaces guest rows for the role). (3) `Dashboard.tsx` `ZonalKpis` type + `ZonalDashboard` component updated — guest cards gone, 2-card zone snapshot (Impact Cells / Total Submissions). (4) `AdminSidebar` `impactZonal` section: `Guests` + `Export CSV` items removed (4 items now; CSV already 403'd for zonals — dead link class). (5) `GuestPolicy::view`/`update`/`viewAny` deny `Impact_Zonal_Coordinator` (was: see/edit ALL guests zone-wide); `GuestController::index` aborts 403 for the role (index() has no policy call). `updateImpactStatus`/`updateFollowUpStatus` covered via the policy row-gate. (6) `ZonalHeader` copy — `'Zone-wide overview'` → `'Your zone overview'` (consistency with the now cells-only scoped surface). **Verified**: `tsc --noEmit` clean; `php -l` clean; tinker payload check — zonal1 (synced 2 cells) → `totalCells=2`, cells grid = ACO/JEDO + APO only, no guest KPI keys, `GuestController::index` 403s; headless-Chrome login as zonal1 → sidebar = Dashboard / Impact Cells / Cell Activity / Leadership Board (no Guests/Export CSV), Zone Snapshot "Impact Cells 2 · assigned to you", cells grid shows only the 2 assigned. Phase 02 verifier 59/0 + `roles:audit` Healthy. Note: the 2 pivot cells on `zonal1@impact.test` are a manual dev-DB assignment for testing (the real admin flow assigns zones via Admin → Users edit); the seeder itself does not assign cells. |
| 13b follow-on — zonal-only signup fix (Inertia setData replace bug) | `resources/js/Pages/Auth/Register.tsx` + `app/Http/Requests/RegisterInertiaRequest.php` | ✅ **Shipped — 2026-08-03.** Bug: `Impact_Zonal_Coordinator`-only signup never went through (no user created, no visible error — the form silently failed). **Root cause**: Inertia 2.3.27 `useForm().setData(object)` **replaces** the entire form data (`commitData(keyOrData)`, no merge — verified in `node_modules/@inertiajs/react/dist/index.js:476`), so `Register.tsx toggleRole()`'s `setData({roles, active_role})` wiped every sibling field (name/email/password/impact_cell_id/leader_*). The submit handler's `String(fd.get(x) ?? prev.x)` then shipped the LITERAL STRING `"undefined"` for the 7 fields absent from the DOM (the cell-setup panel is unmounted for non-`Impact_Leaders` roles), and the server's `uuid` rule on `impact_cell_id` rejected it — with an **invisible** error, because that field's `<InputError>` renders inside the unrendered panel. Leaders signups worked only because those 7 fields happen to be in the DOM for them. **Fix (4 layers)**: (1) `toggleRole` → function-form `setData((prev) => ({...prev, roles, active_role}))` with a full spread + `prev.active_role` read (immune to stale closures); (2) submit handler → `toWire()` sanitizer collapsing `null`/`undefined` → `''` so the wire can never carry `"undefined"`; (3) `HIDDEN_CELL_FIELDS` const (as-const typed) + the 7 hidden fields' `<InputError>`s now render inside the `register-cell-setup-empty` placeholder so a rejection is never a silent "nothing happened"; (4) `RegisterInertiaRequest::prepareForValidation` normalises literal `"undefined"`/`"null"` strings → `null` for the shared `CELL_FIELDS` list (defence-in-depth for hand-rolled clients). Also: password reset moved `onFinish` → `onSuccess` so a failed submit keeps the typed password. **Verified**: `tsc --noEmit` clean; headless-Chrome CDP repro (real browser, Vite HMR) — before: POST carried `"impact_cell_id":"undefined"` + server rejection back to /register with invisible error; after: wire sends `"impact_cell_id":""`, POST → 302 → `/dashboard` rendering the ZONAL COORDINATOR variant (user created + role assigned + logged in). Test users cleaned up. Note: `scripts/verify_phase13b_run.php` [14]/[15] fail on a PRE-EXISTING stale pattern (Admin/UserController sources `cellsList` via `cellsForAssignment()` — behaviorally equivalent — while the verifier asserts the old inline query); unrelated to this fix. |
| 14-18 follow-on — orphan zonal-role cleanup migration (hardening) | `database/migrations/2026_08_03_100000_repair_orphan_impact_zonal_typo_role.php` | ✅ **Shipped — 2026-08-03.** The Phase 14 typo-fix migration (`2026_07_31_150000_fix_impact_zonal_typo_in_roles_table.php`) was RENAME-ONLY: it 503'd `/register` on 2026-08-03 because the typo'd `Impact_Zonal_Cordinator` row existed in a DB state the rename never saw (migration marked Ran before the row existed / DB re-import), then `RolesAndPermissionsSeeder` created the correctly-spelled row as a NEW id, orphaning the typo row (still holding 1 `model_has_roles` assignment). A shipped migration is never edited in place, so the hardening is a NEW idempotent follow-up migration handling EVERY DB state: **State A** clean → no-op (+ normalises `users.active_role` drift); **State B** typo-row-only → rename (original Phase 14 intent); **State C** BOTH rows exist → repoint-or-drop each `model_has_roles` assignment, drop `role_has_permissions` grants, delete the orphan row, normalise `active_role`. Verified: applied cleanly on the live DB, then a tinker simulation recreated all 3 states and ran `up()` — **16/16 checks PASS** (rename leg, orphan leg incl. redundant-drop + repoint + grant cleanup + active_role normalisation, and clean re-run no-op). `down()` is a deliberate no-op (data repair; precedent: flatten-all-cells migration). Supersedes the ad-hoc `scripts/fix_orphan_zonal_role.php` (removed — migration is the single source of truth). |
| 39 Impact_Cell_Admin — Assigned Guests overview + submissions detail links | `app/Http/Controllers/GuestController.php` + `resources/js/Pages/Guests/Assigned.tsx` (NEW) + `resources/js/Pages/Guests/Index.tsx` + `resources/js/Components/AdminSidebar.tsx` + `resources/js/Pages/ImpactSubmissions/Index.tsx` | ✅ **Shipped — 2026-08-03.** User directive (Impact_Cell_Admin role): (1) the Guests surface becomes **"Assigned Guests"** — a per-cell breakdown of ALL Impact Cells with **Total Assigned / Total Contacted / Total Pending** (cells mark guests from Not Contacted → Contacted); (2) `/impact-submissions` gets a **detail view** for each submission. **Changes**: **(A)** `GuestController::assignedGuestsIndex()` — new `Impact_Cell_Admin` branch in `index()`: no `?cell=` → renders `Guests/Assigned`; one `GROUP BY` over `guests` (keyed by `nearest_impact_cell_id`) with `COUNT(*)` + `SUM(CASE WHEN LOWER(TRIM(impact_status)) = 'contacted' …)` (portable + case/whitespace-defensive — `updateImpactStatus` has no enum constraint), then hydrates all cells incl. 0-guest cells; `pending = total − contacted`. `?cell=<uuid>` → renders the legacy `Guests/Index` roster filtered to that cell, read-only (`editableFields=[]` so no dead Edit buttons — `GuestPolicy::update` requires row ownership which cross-cell supervisors lack), with `activeCellName` banner. **(B)** NEW `Guests/Assigned.tsx` — 4 summary cards (Impact Cells / Total Assigned / Total Contacted / Total Pending) + per-cell table (69 rows) with emerald progress bars + per-row View roster links; totals footer. **(C)** `Guests/Index.tsx` — optional `activeCellName` prop: conditional eyebrow/title (`Guests · {cell}`) + indigo banner + "Back to Assigned Guests" link (`data-testid=assigned-back-link`). **(D)** `AdminSidebar` — impactCellAdmin section's `Guests` item renamed **`Assigned Guests`** (admin section's `Guests` untouched). **(E)** `ImpactSubmissions/Index.tsx` — per-row **View details** button (`data-testid=submission-view-{id}`) + clickable Type pill and Cell linking to the EXISTING `impact-submissions.show` detail page (Meta + Submitted Data cards — the gap was discoverability, not the page). **(F) Inline expandable rosters** (2026-08-03 follow-up) — Assigned Guests rows now expand INLINE via a chevron (single-open accordion) without leaving the page: new `GET /guests/roster` JSON endpoint (`GuestController::roster()`, registered BEFORE `/guests/{id}` so 'roster' isn't captured as an id; Impact_Cell_Admin-only 403 gate; returns projected guest fields capped at 100) lazy-loads each cell's roster on first expand, cached per cell afterwards; native `fetch()` (SoulSearch pattern); per-cell loading/error/idle states with a Retry that re-fetches (dedicated `loadRoster` kept OUT of the React state updater — pure updaters, no StrictMode double-fetch; Retry calls `loadRoster` not `toggle`, so it re-fetches instead of collapsing). **(G) `impact_status` enum constraint** (2026-08-03 follow-up) — only `Contacted` / `Not Contacted` / `Not Reachable` may be persisted: new `Guest::IMPACT_STATUSES` const (single source of truth; plain array so the nullable varchar column + legacy/CSV rows stay readable) enforced on BOTH write paths — `updateImpactStatus` (now coerces `''` → null BEFORE validation so the Clear path stays valid, then `['nullable','string','max:64', Rule::in(Guest::IMPACT_STATUSES)]`) and `GuestRequest` (the full-edit path, so the form can't bypass the enum). `Guests/Edit.tsx` free-text `impact_status` input → `ImpactStatusSelect` (prop-driven options defaulting to the canonical 3; a legacy DB value renders as a disabled "legacy" option so it can't be re-saved into a 422). CSV import verified as a non-path (only writes guest_name/phone/email/event/source — never impact_status), so the enum covers every persistence path. `verify_phase07_run.php` [18] updated to assert the new rule. Verified: tinker Validator matrix — 3 canonical values + null + '' pass, `CONTACTED` (upper) + garbage rejected; `php -l` + `tsc` clean; Guest tests 3/3; phase07 verifier 20/21 with the [3] admin-section 13-vs-14 failure PRE-EXISTING (Phase 33 added the Profile item; unrelated to this change). **Verified**: `php -l` + `tsc --noEmit` clean; Guest feature tests 3/3; headless-Chrome as a temp Impact_Cell_Admin with demo data (ACO/JEDO=2 guests [1 Contacted, 1 Not Contacted], APO=2 [1 Contacted, 1 unset]) — sidebar shows Assigned Guests (no plain Guests), overview rows ACO/JEDO 2/1/1 + APO 2/1/1 + all 69 cells, server totals assigned=4 contacted=2 pending=2, roster drill-down → "Guests · ACO/JEDO" banner + 2 rows + back link, submissions list → View details → detail page (Meta + Data cards render). Demo data + temp user cleaned up afterwards. |

## 2. Quick start (5 min)

```bash
cd C:\laragon\www\impact_portal_plus

# 1. Confirm Laravel boots and DB is reachable (current APP_URL is http://impact_portal_plus.test)
php artisan --version                        # → Laravel Framework 12.64.0
php artisan db:show                          # → impact_guest / 15+ tables

# 2a. Run all 5 verification suites
for s in 02 03 04 05 06; do php scripts/verify_phase0${s}_run.php || { echo "Phase ${s} regressed"; exit 1; }; done
# MUST print "58 pass / 0 fail", "15 pass / 0 fail", "36 pass / 0 fail", "22 pass / 0 fail", "14 pass / 0 fail"

# 3. Browse the app via Laragon's vhost
#    http://impact_portal_plus.test/         (welcome page)
#    http://impact_portal_plus.test/login    (Breeze login — see full test-user table below)
```

### Test credentials (all seeded users)

| Email                                            | Password                 | Role(s)                           | Group           | Created by               | What you'll see                                                                                                                                        |
| ------------------------------------------------ | ------------------------ | --------------------------------- | --------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `sbcadmin@impact.test`                           | `//Chris##101`           | Administrator + Impact_Cell_Admin | (none — admin)  | `AdminUserSeeder`        | Full admin dashboard; role-switcher in top-bar shows both roles                                                                                        |
| `officer1@impact.test`                           | `//Officer##101`         | FollowUpOfficer                   | followUpOfficer | `FollowUpOfficerSeeder`  | Officer dashboard + 5 KPI cards + top-8 guest queue + 5 seeded guests (one per `contacted_status` permutation)                                         |
| `followUpAdmin@impact.test`                      | `//Admin##101`           | Follow_UP_Admin                   | followUpOfficer | `FollowUpOfficerSeeder`  | Officer dashboard variant; the only officer role that may reassign guests to a different officer (per-role special case in `RoleHelper::canEditField`) |
| `phase04-test-Administrator@impact.test`         | `//Administrator##101`   | Administrator                     | (none)          | `verify_phase04_run.php` | Admin fixture for `prepareForValidation` pass-through + `GuestResource` admin-only timestamp exposure                                                  |
| `phase04-test-Follow_UP@impact.test`             | `//Follow_UP##101`       | Follow_UP                         | followUpTeam    | `verify_phase04_run.php` | Team fixture for strip policy on `phone`/`impact_status`                                                                                               |
| `phase04-test-Impact_Leaders@impact.test`        | `//Impact_Leaders##101`  | Impact_Leaders                    | impactCell      | `verify_phase04_run.php` | ImpactCell fixture for the cell-group strip policy                                                                                                     |
| `phase04-test-FollowUpOfficer@impact.test`       | `//FollowUpOfficer##101` | FollowUpOfficer                   | followUpOfficer | `verify_phase04_run.php` | Officer fixture for cross-cutting rule (`contacted_status=AvailableForVisit` keeps `visitation_status` + `feedback`)                                   |
| `phase04-test-FollowUpOfficer-OTHER@impact.test` | `//FollowUpOfficer##101` | FollowUpOfficer                   | followUpOfficer | `verify_phase04_run.php` | Second officer (doesn't own the seeded guests) for `GuestPolicy::view` denial test                                                                     |
| `team1@impact.test`                              | `//Team##101`            | Follow_UP                         | followUpTeam    | `FollowUpTeamSeeder`     | Team dashboard + team queue with inline status editing + "Mark Contacted" quick-set buttons                                                            |
| `teamViewOnly@impact.test`                       | `//ViewOnly##101`        | Follow_UP_View_Only               | followUpTeam    | `FollowUpTeamSeeder`     | Same team dashboard but amber View-only banner + all controls disabled                                                                                 |

**Convention:** every password follows `//<RoleName>##101` where `<RoleName>` matches the Spatie role assigned to that user. The literal `//`, `#`, and trailing digits are part of the plaintext — the User model's `'hashed'` cast handles bcrypt so DB never stores plaintext.

**Where they're seeded:** `database/seeders/AdminUserSeeder.php` (admin), `database/seeders/FollowUpOfficerSeeder.php` (officer1 + followupAdmin), `database/seeders/FollowUpTeamSeeder.php` (team1 + teamViewOnly), `scripts/verify_phase{02,03,04,05}_run.php` (Phase 04 fixtures, idempotent via `User::firstOrCreate(['email' => …])`). Re-running any seeder or verifier leaves existing rows untouched.

**Two ways to log in:**

1. Use the page UI: `http://impact_portal_plus.test/login`
2. Script-driven: `php artisan tinker --execute="auth()->loginUsingId(\App\Models\User::where('email','sbcadmin@impact.test')->first()->id)"` — useful for verifier scripts.

If any verifier doesn't pass, **do not proceed** — fix the regression first. See § 8.

---

## 2b. Production deploy — after every `git pull` (Hostinger)

> The everyday prod update flow is `git pull` + the block below. This is the
> **verified** command set for the production box (plain `php`/`npm` do NOT
> resolve in the SSH shell — use the full alt-php84 path + export the
> alt-nodejs22 PATH). Same block lives at the top of
> `scripts/HOSTINGER_DEPLOY_CHEATSHEET.md`.

```bash
# 1. SSH in
ssh u188660189@ssh.hostinger.com

# 2. Project root — where composer.json lives (run `pwd` to confirm)
cd /home/u188660189/domains/app.summitdata.one/public_html

# 3. Full-path binaries
PHP=/opt/alt/php84/usr/bin/php
export PATH=/opt/alt/alt-nodejs22/root/bin:$PATH

# 4. Pull the update branch
git pull origin update

# 5. Composer + frontend build (skip npm if no JS/React changes in the pull)
$PHP /opt/alt/php84/usr/bin/composer install --optimize-autoloader --no-dev --no-interaction --prefer-dist
npm ci --no-audit --no-fund && npm run build

# 6. Migrate + idempotent seeders (both safe to re-run)
$PHP artisan migrate --force --no-interaction
$PHP artisan db:seed --class=RolesAndPermissionsSeeder --force
$PHP artisan db:seed --class=ImpactCellSeeder --force

# 7. Cache trio + storage link (RE-RUN after any .env change too!)
$PHP artisan config:cache && $PHP artisan route:cache && $PHP artisan view:cache && $PHP artisan storage:link

# 8. Permissions
chmod -R 775 storage bootstrap/cache && chown -R u188660189:www-data storage bootstrap/cache

# 9. Verify
curl -s -o /dev/null -w 'home: %{http_code}\n' https://app.summitdata.one/
curl -s -o /dev/null -w 'register: %{http_code}\n' https://app.summitdata.one/register
#    Expect 200 for both. Hard-refresh the browser (Ctrl-Shift-R) after.
```

**Why the two seeders are in every pull:** both are `firstOrCreate`-idempotent,
so re-running is a no-op on healthy DBs but repairs the two incidents seen in
production — a missing `roles` row makes `/register` return **503** (Phase 14
guard), and a missing `impact_cells` row empties the register page's cell
dropdown.

**Maintenance-mode gotcha:** if `$PHP artisan down` is ever run before a
deploy, the block above does NOT re-enable the app — `$PHP artisan up` must be
the last step. The site was once found stuck in `down` (all pages 503).

---

## 3. Tech stack

| Layer          | Choice                                             | Why                                                                                                                                         |
| -------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| HTTP framework | **Laravel 12**                                     | Per user confirmation; idiomatic PHP web framework                                                                                          |
| Auth           | **Breeze (React + TS) + Sanctum stateful**         | Inertia pairs naturally with session cookies; no JWT tokens                                                                                 |
| Frontend       | **Inertia.js v2 + React 18 + Vite + Tailwind 3.2**  | SPA feel without an API layer; controllers return `Inertia::render('Page', props)`. (Radix + shadcn-style primitives planned for Phase 04+) — **post-Phase-06 polish**: design tokens consolidated into `tailwind.config.js` (see `Phase_06b-06c_UI_Polish.md` § 2.1) |
| ORM            | **Eloquent** with UUID PKs                         | Replaces Prisma                                                                                                                             |
| Roles          | **Spatie Laravel-Permission** for 9 roles          | DB-backed `roles` table; pivot table auto-managed                                                                                           |
| Groups (3)     | **`app/Support/RoleHelper.php`** (custom)          | Lives in code, not DB — 3 arrays + a column-access matrix                                                                                   |
| Audit          | **Spatie Laravel-Activitylog**                     | Native Eloquent observer; replaces old custom `AuditLog` table                                                                              |
| Cache          | **`Cache::remember`** with `file` driver           | No custom `DashboardCache` table                                                                                                            |
| CSV import     | **maatwebsite/excel (Laravel Excel)**              | Built-in queue/batch/chunk                                                                                                                  |
| DB             | **MySQL 8.4** at `localhost:3306` (`impact_guest`) | Uses `caching_sha2_password`; PHP PDO/mysqlnd handles it natively                                                                           |

The complete mapping table (Node/Express → Laravel) is in `Implementation/00_Laravel_Bridge.md` § 2.

---

## 4. The 9 roles + 3 groups (glossary)

The **9 Spatie roles** are seeded by `RolesAndPermissionsSeeder`:

```
Administrator       Supervisor         FollowUpOfficer
Follow_UP           Follow_UP_Admin    Follow_UP_View_Only
Impact_Leaders      Impact_Cell_Admin  Impact_Cell_Report
```

They collapse into **3 functional groups** (defined in `RoleHelper::GROUP_*` constants):

| Group key         | Members                                                     | Owns these Guest columns                                                                                                                                                                            |
| ----------------- | ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `impactCell`      | `Impact_Leaders`, `Impact_Cell_Admin`, `Impact_Cell_Report` | `impact_status`, `nearest_impact_cell_id`                                                                                                                                                           |
| `followUpOfficer` | `FollowUpOfficer`, `Follow_UP_Admin`                        | `gender`, `marital_status`, `age`, `phone`, `address`, `contacted_status`, `join_when`, `days_available`, `comments`, `visited`, `visited_at`, `indicated_to_join`, `visitation_status`, `feedback` |
| `followUpTeam`    | `Follow_UP`, `Follow_UP_View_Only`                          | `follow_up_status`, `follow_up_contacts`                                                                                                                                                            |
| (no group)        | `Administrator`, `Supervisor`                               | `Administrator` writes everything; `Supervisor` writes nothing on Guest                                                                                                                             |

**Note (post-Phase-05 fix):** all matrix KEY names are **snake_case** to match the production HTTP wire format (`$request->all()` returns snake_case, DB columns are snake_case, GuestResource outputs snake_case, frontend types are snake_case). An earlier v1 of this file had camelCase keys and was silently stripping every multi-word field from FollowUpOfficer writes — fixed in the post-Phase-05 commit. A regression guard in `verify_phase02_run.php` / `verify_phase04_run.php` fails fast if a future contributor types camelCase.

Source of truth: `app/Support/RoleHelper.php` (GROUP_GUEST_OWNER matrix) + `Implementation/03_Three_User_Groups.md` (design intent).

---

## 5. File map (where to find what)

```
impact_portal_plus/
├── HANDOFF.md                                  ← you are here
├── README.md                                   ← Laravel default (not project-specific)
│
├── Plan/                                       ← ORIGINAL design intent (do not edit)
│   ├── Functional_Documentation/   (14 docs: system overview, roles, workflows, lifecycle, ...)
│   └── Technical_Documentation/    (21 docs: architecture, schema, API, ...)
│
├── Implementation/                              ← build-phase docs + the bridge
│   ├── 00_Vision.md
│   ├── 01_Architecture.md
│   ├── 02_Database_Schema.md
│   ├── 03_Three_User_Groups.md     ← column-edit matrix source of truth
│   ├── 04_Impact_Cell_Hierarchy.md ← Phase 03 reference
│   ├── 05_Leadership_Board.md
│   ├── 06_Dashboard_Design_System.md
│   ├── Phase_01_Foundation.md
│   ├── Phase_02_Auth_And_Users.md
│   ├── Phase_03..12_*.md           ← future phases
│   └── 00_Laravel_Bridge.md        ← THE tech-mapping doc (read this for Laravel idioms)
│
├── app/
│   ├── Models/User.php                                  ← HasRoles + activeRole() accessor
│   ├── Models/ImpactCell.php                            ← UUID PK, parent() + subCells() self-relations, hierarchyRulesOrThrow()
│   ├── Http/Controllers/Auth/RoleSwitchController.php   ← POST /auth/switch-role
│   ├── Http/Controllers/ImpactCellController.php       ← CRUD with pre-check + delete + abort(409)
│   ├── Http/Middleware/HandleInertiaRequests.php        ← shares auth.user.{activeRole,roles,hasMultipleRoles}
│   ├── Support/RoleHelper.php                           ← 3 groups + matrix + stripDisallowed
│   └── ...  (Breeze Auth/Profile controllers)
│
├── database/
│   ├── migrations/
│   │   ├── 0001_01_01_000000_create_users_table.php                  (Breeze — includes `sessions` table for SESSION_DRIVER=database)
│   │   ├── 0001_01_01_000001_create_cache_table.php                  (Breeze — cache + cache_locks)
│   │   ├── 0001_01_01_000002_create_jobs_table.php                   (Breeze — jobs + job_batches + failed_jobs)
│   │   ├── 2026_07_26_120000_add_active_role_to_users_table.php      ← Phase 02
│   │   ├── 2026_07_26_130000_create_permission_tables.php            ← Spatie (copied from stub)
│   │   ├── 2026_07_26_130001_create_activity_log_table.php           ← Spatie (copied from stub)
│   │   └── 2026_07_27_120000_create_impact_cells_table.php           ← Phase 03
│   └── seeders/
│       ├── DatabaseSeeder.php                  ← calls all seeders below
│       ├── RolesAndPermissionsSeeder.php       ← 9 roles
│       ├── AdminUserSeeder.php                 ← sbcadmin@impact.test //Chris##101
│       ├── ImpactCellSeeder.php                ← 69 cells: 65 primary + 4 APO sub-cells
│       ├── FollowUpOfficerSeeder.php           ← Phase 05 — officer1 + followUpAdmin
│       └── FollowUpTeamSeeder.php              ← Phase 06 — team1 + teamViewOnly
│
├── config/permission.php   ← Spatie Permission config (copied from stub)
├── routes/web.php          ← has /auth/switch-role + 5 /impact-cells + 6 /guests + PATCH /guests/{id}/follow-up-status routes under auth middleware
├── scripts/
│   ├── verify_phase02_run.php    ← 58-sub-assertion Phase 02 verifier (incl. [1b] snake_case-shape guard)
│   ├── verify_phase03_run.php    ← 15-sub-assertion Phase 03 verifier (7 numbered cases, some multi-check)
│   ├── verify_phase04_run.php    ← 36-sub-assertion Phase 04 verifier (20 numbered cases, guests FK + stripping + Resource + Policy)
│   ├── verify_phase05_run.php    ← 22-sub-assertion Phase 05 verifier (officer KPIs + reassign + seeder idempotency)
│   ├── verify_phase06_run.php    ← 14-sub-assertion Phase 06 verifier (team users + policy + team variant)
│   └── probe_password_hash.php   ← confirms the 'hashed' cast fires correctly
├── resources/js/
│   ├── Components/RoleBadge.tsx               ← top-bar role badge (Phase 02 UI wire)
│   ├── Components/RoleSwitcher.tsx            ← dropdown of roles; visible only when hasMultipleRoles
│   ├── Components/KPICard.tsx                 ← reusable KPI metric card (Phase 05)
│   ├── Components/StatusPill.tsx              ← colored status badge (Phase 05)
│   ├── Components/ContactsTimeline.tsx        ← vertical timeline for follow_up_contacts (Phase 06)
│   ├── Components/ViewOnlyBanner.tsx          ← amber banner for Follow_UP_View_Only (Phase 06)
│   ├── Layouts/AuthenticatedLayout.tsx        ← RoleBadge in dropdown trigger + RoleSwitcher in header; both also in mobile menu
│   ├── Pages/Dashboard.tsx                    ← officer / team / admin variants (Phase 05 + 06)
│   ├── Pages/Guests/Index.tsx                 ← paginated guest list
│   ├── Pages/Guests/Show.tsx                  ← read-only guest detail + ContactsTimeline
│   ├── Pages/Guests/Edit.tsx                  ← role-scoped edit form + contact sections editor
│   ├── Pages/ImpactCells/Index.tsx            ← Phase 03 stub — totals + Phase 04 note
│   └── Pages/ImpactCells/Show.tsx             ← Phase 03 stub — cell details + sub-cells list
└── public/  resources/  storage/  tests/   ← standard Laravel + Breeze frontend
```

---

## 6. What's built (Phase 02 + 02b-UI + Phase 03 deliverables)

| Concern                                                          | File                                                                       | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Phase 02 — auth + RBAC + active-role**                         |                                                                            |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `users.active_role` column                                       | `database/migrations/2026_07_26_120000_add_active_role_to_users_table.php` | Nullable string, after `password`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| User model with `HasRoles`                                       | `app/Models/User.php`                                                      | Adds Spatie roles + 3 helpers: `activeRole()`, `canSwitchTo($role)`, `activeGroup()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Active-role accessor (single source of truth)**                | `User::activeRole()`                                                       | Resolves `$user->active_role` if it's still in the user's Spatie roles, else first Spatie role, else `null` — handles stale-column fallback defensively. **USE THIS everywhere** instead of inlining `?? $user->getRoleNames()->first()`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 3-group RBAC matrix                                              | `app/Support/RoleHelper.php`                                               | `GROUP_IMPACT_CELL`, `GROUP_FOLLOW_UP_OFFICER`, `GROUP_FOLLOW_UP_TEAM` + `GROUP_GUEST_OWNER` matrix + `groupOf()`, `canEditField()`, `stripDisallowed()`, `allGroupOwnedFields()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Spatie config                                                    | `config/permission.php`                                                    | Default Spatie config (copied from stub)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Roles table + pivot tables                                       | `database/migrations/2026_07_26_130000_create_permission_tables.php`       | `roles`, `permissions`, `model_has_roles`, `model_has_permissions`, `role_has_permissions` (copied from stub)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Activity log table                                               | `database/migrations/2026_07_26_130001_create_activity_log_table.php`      | Spatie's `activity_log` table (copied from stub)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 9 roles seeded                                                   | `database/seeders/RolesAndPermissionsSeeder.php`                           | Calls `forgetCachedPermissions()` before & after per bridge §7 pitfall #7                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Admin user                                                       | `database/seeders/AdminUserSeeder.php`                                     | `sbcadmin@impact.test` / `//Chris##101` / Administrator / `active_role = Administrator`. **Password is auto-hashed by the `'hashed'` cast in `User::casts()` — never `Hash::make()` here.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Switch-role endpoint                                             | `app/Http/Controllers/Auth/RoleSwitchController.php` + `routes/web.php`    | `POST /auth/switch-role` under `auth` middleware. Validates via `User::canSwitchTo()`. `back(303)->with('success', ...)` so Inertia partial-reloads pick up the new active role.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `.env` config                                                    | `.env`                                                                     | MySQL: `ipcDBurs22@127.0.0.1:3306/impact_guest` (password `Ldywgw^5676GGH` — `^` is literal). `APP_URL=http://impact-portal.test`. `SESSION_DRIVER=database`. `CACHE_STORE=file`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Storage symlink                                                  | `public/storage` → `storage/app/public`                                    | Created via `php artisan storage:link` per bridge §4 step 5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Phase 02b — UI wire**                                          |                                                                            |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Inertia shared props                                             | `app/Http/Middleware/HandleInertiaRequests.php`                            | `share()` now adds `auth.user.{activeRole, roles, hasMultipleRoles}` so the frontend reads single-source-of-truth role data via `usePage().props.auth.user.*`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Role badge component                                             | `resources/js/Components/RoleBadge.tsx`                                    | Inline pill next to user name in the dropdown trigger; role-specific color; shows "No role" if activeRole is null                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Role switcher component                                          | `resources/js/Components/RoleSwitcher.tsx`                                 | Dropdown of `user.roles`; visible only when `hasMultipleRoles`; POSTs to `/auth/switch-role` via Inertia's `router.post`; preserves scroll + state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Top-bar wiring                                                   | `resources/js/Layouts/AuthenticatedLayout.tsx`                             | Renders `<RoleBadge>` in user dropdown trigger + `<RoleSwitcher>` next to it (desktop). Both also appear in the mobile responsive menu (the menu shows a role list with the active one checked).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Phase 03 — Impact Cell hierarchy**                             |                                                                            |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `impact_cells` table                                             | `database/migrations/2026_07_27_120000_create_impact_cells_table.php`      | UUID PK, `name` unique, `phone`/`address` nullable, `parent_cell_id` (FK self with `restrictOnDelete()`), `is_primary` boolean, `order` int, indexes on `parent_cell_id`, `is_primary`, and `(is_primary, order)` composite                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ImpactCell model                                                 | `app/Models/ImpactCell.php`                                                | UUID PK via `HasUuids`, `parent(): BelongsTo` + `subCells(): HasMany` self-relations, two validators (`hierarchyRules()` returning array + `hierarchyRulesOrThrow()` unified throw), `ensureParentIsPrimary()`, `scopePrimary()` + `scopeOrdered()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ImpactCell seeder                                                | `database/seeders/ImpactCellSeeder.php`                                    | 68 names from `Plan/Technical_Documentation/Appendix.md` + 1 added "APO" so the split demo has a parent → **69 total: 65 primary + 4 APO sub-cells**. Idempotent via `firstOrCreate(['name' => …])`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ImpactCell controller                                            | `app/Http/Controllers/ImpactCellController.php`                            | `index`/`show`/`store`/`update`/`destroy`. `destroy()` uses `abort(409)` (not try/catch QueryException — a blanket catch would mask future FK violations from `ImpactSubmissions.impact_cell_id` once Phase 04+ ships).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ImpactCell routes                                                | `routes/web.php`                                                           | 5 routes under `auth` middleware: `GET/POST /impact-cells`, `GET/PUT/DELETE /impact-cells/{id}`. Admin-only check deferred to `ImpactCellPolicy` (Phase 03 follow-up).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Inertia stub pages                                               | `resources/js/Pages/ImpactCells/{Index,Show}.tsx`                          | Phase 03 stubs render totals + cell details. Phase 04 will flesh out the full admin tree view.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Phase 04 — Guest CRUD + column policy**                        |                                                                            |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `guests` table                                                   | `database/migrations/2026_07_27_130000_create_guests_table.php`            | UUID PK, every column from `Implementation/02_Database_Schema.md`, `softDeletes()`, `nearest_impact_cell_id` FK nullable `restrictOnDelete`, `follow_officer_id` FK nullable `restrictOnDelete` (`foreignId` = `unsignedBigInteger` for compatibility with `users.id`), `follow_up_contacts` JSON (max 3 sections), 7 indexes including composite `[follow_officer_id, deleted_at]`.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Guest model                                                      | `app/Models/Guest.php`                                                     | UUID PK via `HasUuids` + `SoftDeletes` traits, `nearestImpactCell(): BelongsTo ImpactCell`, `followOfficer(): BelongsTo User`, scopes `scopeAssignedTo($userId)` + `scopeForImpactCell($cellId)`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Guest form request                                               | `app/Http/Requests/GuestRequest.php`                                       | `prepareForValidation()` calls `RoleHelper::stripDisallowed($this->user()?->activeRole(), $this->all())` + `$this->replace(...)`. **Single source of truth**: column-write decisions live in `RoleHelper::GROUP_GUEST_OWNER`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Guest resource                                                   | `app/Http/Resources/GuestResource.php`                                     | `deleted_at` + `updated_at` + `created_at` are null for non-admin (per spec "Hidden from non-admins: ... raw timestamps"). All other fields returned as-is.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Guest policy                                                     | `app/Policies/GuestPolicy.php`                                             | `view` / `create` / `update` / `delete` by role + group. **Officer**: their `follow_officer_id` matched. **ImpactCell**: their `impact_cell_id` matched (column will be added in Phase 07 — silent fallback today). **followUpTeam**: read-only. Admin: full.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Guest controller                                                 | `app/Http/Controllers/GuestController.php`                                 | `index`/`show`/`store`/`update`/`destroy` returning `Inertia::render('Guests/{Index,Show}', ...)`. **Cross-cutting rule**: if `contacted_status != AvailableForVisit`, `visitation_status` + `feedback` are nulled server-side (business rule). Self-assign on `store()` for non-admin.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ImpactCell policy                                                | `app/Policies/ImpactCellPolicy.php`                                        | Phase 03 follow-up: only Administrator can `update`. (Read-only for everyone else.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Inertia stub pages                                               | `resources/js/Pages/Guests/{Index,Show}.tsx`                               | Phase 04 stubs render the role-aware column set. Phase 05+ will flesh out the per-group dashboards + inline editing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Phase 04b — Local infrastructure (Laragon bridge)**            |                                                                            |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Parent `.htaccess`                                               | `C:\laragon\www\impact_portal_plus\.htaccess`                              | **Purpose**: Laragon auto-vhost (`DocumentRoot=project parent`) cannot reach Laravel's `public/` entry. This file bridges parent → `public/` so the auth pages load. **Security**: the rewrite is **unconditional** for non-`/public/` paths — `GET /composer.json`, `/HANDOFF.md`, `/app/*`, `/routes/*`, etc. all forward to `public/index.php` → Laravel router → 404. Belt-and-suspenders `RedirectMatch 404 /\.(?!well-known/)` short-circuits dot-files (`/.env`, `/.git/HEAD`) at the URI translation phase so they never reach the rewrite chain. `Options -Indexes` prevents directory listings. **Production**: in production the vhost's `DocumentRoot` is properly set to `public/`, so this file lives outside the served tree and is never read by Apache — no behavior change, zero risk. |
| **Phase 05 — Follow Up Officer dashboard + reassign permission** |                                                                            |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Officer dashboard controller                                     | `app/Http/Controllers/DashboardController.php`                             | `index()` computes 5 KPIs (`pendingContacts`, `totalCalls`, `visited`, `pendingVisit`, `responseRate`) + top-8 queue when the active role is in `GROUP_FOLLOW_UP_OFFICER`; falls through to the admin welcome otherwise. Inertia page = `Dashboard` with the matching props. Wired in `routes/web.php` (was an inline closure before).                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| RoleHelper reassign exception                                    | `app/Support/RoleHelper.php`                                               | `canEditField()` now special-cases the `follow_officer_id` column: only `Administrator` + `Follow_UP_Admin` may reassign (per `Implementation/03` matrix table). The matrix for the Follow Up Officer group stays unchanged; self-assign in `GuestController::store` overrides any client-provided value for non-admin. Documented in the file docblock.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `FollowUpOfficerSeeder`                                          | `database/seeders/FollowUpOfficerSeeder.php`                               | Seeds two test users so the verifier + manual QA have a reliable state: `officer1@impact.test` (`//Officer##101`, `FollowUpOfficer`, with 5 assigned guests spanning each `contacted_status`) + `followUpAdmin@impact.test` (`//Admin##101`, `Follow_UP_Admin`). Idempotent via `firstOrCreate`. Wired into `DatabaseSeeder`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Reusable components                                              | `resources/js/Components/{KPICard,StatusPill}.tsx`                         | `KPICard`: 12px radius, caption + big number + trend slot, dark-mode-aware surface + inner ring. `StatusPill`: 11px font with role/group color hints (Pending = neutral, Visited = green, Issue = red).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Role-aware top-bar nav                                           | `resources/js/Layouts/AuthenticatedLayout.tsx`                             | Three nav states: **Admin**: Dashboard, Guests, Impact Cells, Profile. **Follow Up Officer / Team / Impact Leader**: Dashboard, My Guests, Profile (admin links hidden when active role is non-admin). Selection driven by `auth.user.activeGroup` from `HandleInertiaRequests` shared props — single source of truth.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Officer variant page                                             | `resources/js/Pages/Dashboard.tsx`                                         | Branches on `auth.user.activeGroup`: officer → 5 KPI cards + top-8 queue (sorted NOT CONTACTED first) + role-aware empty state. Otherwise → admin welcome.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Phase 06 — Follow Up Team dashboard**                          |                                                                            |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Team dashboard controller                                        | `app/Http/Controllers/DashboardController.php`                             | `index()` now dispatches `teamDashboard()` when `activeGroup = 'followUpTeam'`. Computes 4 team KPIs (`pendingContacts`, `contactedToday`, `wrongNumber`, `notReachable`) scoped to ALL guests + team queue sorted by `follow_up_status` priority (NOT CONTACTED → CONTACTED → WRONG NUMBER / NOT REACHABLE → others), limited to 20 rows, with officer name eager-loaded.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Inline follow_up_status endpoint                                 | `app/Http/Controllers/GuestController.php` + `routes/web.php`              | `PATCH /guests/{id}/follow-up-status` — lightweight JSON endpoint only accepts `follow_up_status`. Used by the team dashboard's inline dropdown and "Mark Contacted" quick-set button. Authorizes via `GuestPolicy::update` so Follow_UP is allowed, Follow_UP_View_Only blocked.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| GuestPolicy update for Follow_UP                                 | `app/Policies/GuestPolicy.php`                                             | `update()` now includes a `Follow_UP` branch that returns `true` (team sees all guests; column-level write restriction is enforced by the Form Request). `Follow_UP_View_Only` stays blocked (returns `false` via the `default` branch). `view()` for `followUpTeam` now returns `true` (unrestricted team view).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `FollowUpTeamSeeder`                                             | `database/seeders/FollowUpTeamSeeder.php`                                  | Seeds `team1@impact.test` (`//Team##101`, Follow_UP) + `teamViewOnly@impact.test` (`//ViewOnly##101`, Follow_UP_View_Only) + 5 marker guests with distinct `follow_up_status` values (`NULL`, `NOT CONTACTED`, `CONTACTED`, `WRONG NUMBER`, `NOT REACHABLE`). Idempotent via `firstOrCreate` + marker-guarded `forceDelete`. Wired into `DatabaseSeeder`.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ContactsTimeline component                                       | `resources/js/Components/ContactsTimeline.tsx`                             | Vertical timeline visualising `follow_up_contacts[]` (max 3 sections). **Edit mode** (for Follow_UP in the Edit page): date picker + contact-type select + comments textarea per section, with add/remove controls. **Read-only mode** (for other groups in the Show page): displays the timeline without edit controls.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ViewOnlyBanner component                                         | `resources/js/Components/ViewOnlyBanner.tsx`                               | Amber warning banner rendered when `activeRole === 'Follow_UP_View_Only'`. Shown on Dashboard + Show page. Includes a warning icon and explanatory text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Team variant page                                                | `resources/js/Pages/Dashboard.tsx`                                         | Branches on `variant = 'team'`: 4 KPI cards (Pending Contacts, Contacted Today, Wrong Number, Not Reachable) + team queue data table with inline `follow_up_status` select + "Mark Contacted" quick-set button. ViewOnlyBanner at top when Follow_UP_View_Only. All controls disabled for view-only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Show page ContactsTimeline                                       | `resources/js/Pages/Guests/Show.tsx`                                       | Now imports + renders `<ViewOnlyBanner>` at the top of the page, and `<ContactsTimeline contacts={guest.follow_up_contacts} readOnly={true} />` in the Follow Up Team group section.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Edit page contact sections                                       | `resources/js/Pages/Guests/Edit.tsx`                                       | Follow Up Team fieldset now includes a `<ContactsTimeline>` in edit mode for `follow_up_contacts`. `follow_up_status` uses a select instead of text input. Controlled by `canEdit('follow_up_status')` and `canEdit('follow_up_contacts')` per the editable fields pattern.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

---

## 7. Verification (must stay green)

```bash
# Phase 02 — 58-sub-assertion regression net — exits non-zero on any regression
#   (includes [1b]: 18-row snake_case-shape guard covering every GROUP_GUEST_OWNER key)
php scripts/verify_phase02_run.php

# Phase 03 — 15-sub-assertion regression net — asserts hierarchy + split + idempotency
php scripts/verify_phase03_run.php

# Phase 04 — 36-sub-assertion regression net — guests FK + prepareForValidation stripping + Resource masking + Policy
php scripts/verify_phase04_run.php

# Phase 05 — 22-sub-assertion regression net — officer KPIs + reassign permission + nav scoping
php scripts/verify_phase05_run.php

# Phase 06 — 14-sub-assertion regression net — team users, policy assertions, team dashboard variant
php scripts/verify_phase06_run.php

# Probe — confirms the 'hashed' cast fires for the seeded admin password
php scripts/probe_password_hash.php
```

**Phase 02 verifier asserts**: 9 roles seeded (with exact names), admin user has Administrator + active_role, `RoleHelper::groupOf` 11-case matrix, `canEditField` 11-case policy, `stripDisallowed` 4-case (Admin pass-through / Officer strip / null + unknown-role defensive), `/auth/switch-role` route registered, `allGroupOwnedFields` ≥ 10 field names.

**Phase 03 verifier asserts**: 69 rows seeded, 65 primary + 4 sub-cells, hierarchy invariant (every non-primary has `parent_cell_id`; no primary has a parent), the APO primary has exactly the 4 expected sub-cells, `subCells()->exists()` is true for APO (controller would 409) and false for a leaf primary, seeder idempotency (count + split re-applied on re-run).

**Phase 04 verifier asserts**: guests table + every required column exists, both FKs use `restrictOnDelete`, `GuestRequest::prepareForValidation` strips per-group (FollowUpOfficer → keeps officer cols / drops impact_status + follow_up_status + admin-only; Follow_UP → keeps team cols / drops phone + impact_status; Impact_Leaders → keeps impact_cell cols / drops phone + follow_up_status; Administrator → pass-through), cross-cutting rule zeroes `visitation_status` + `feedback` when `contacted_status != AvailableForVisit`, `GuestResource` masks `created_at` / `updated_at` / `deleted_at` for non-admin + exposes them for admin, `GuestPolicy::delete` denies non-admin, `GuestPolicy::view` allows admin + assigned officer / denies unassigned officer, `ImpactCellPolicy::update` allows only Administrator, all 5 guest routes + 5 impact-cell routes registered.

**Phase 05 verifier asserts**: 2 officer test users seeded with the right roles + active*role, officer1 has 5 assigned guests across all 5 `contacted_status` permutations, none have `deleted_at`, `RoleHelper::canEditField` Special-Case holds (Administrator = true, Follow_UP_Admin = true, plain FollowUpOfficer = false, Impact / Team groups = false, Supervisor / null / unknown = false), `DashboardController` route registered, KPI math is exact (Pending Contacts + Total Calls + Visited + Pending Visit + Response Rate all match the seeded counts), officer queue length respects `LIMIT 8` (capped at the seeded 5 in test data — in production it tops out at 8 once a real officer gets more assigned guests) + NOT CONTACTED first, role-aware top-bar nav exposes `My Guests` link for officer role only (admin / supervisor / no role = no officer nav) — \_manual QA only, no verifier coverage yet* (the `navItemsFor()` branch in `AuthenticatedLayout.tsx` is wired but not asserted by any Phase 05 sub-check), no `.env` / `public/hot` / `vendor` / `node_modules` in any verifier intermediate state.

**Phase 06 verifier asserts**: 2 team test users (`team1@impact.test` with Follow_UP + `teamViewOnly@impact.test` with Follow_UP_View_Only) seeded with correct roles + active_role, 5 marker guests covering all `follow_up_status` permutations, `GuestPolicy::update` allows Follow_UP (returns true) + blocks Follow_UP_View_Only (returns false), `GuestPolicy::view` allows Follow_UP for any guest, `RoleHelper::groupOf` returns `'followUpTeam'` for both Follow_UP and Follow_UP_View_Only, `DashboardController` returns `variant = "team"` for Follow_UP user with KPIs present.

**Run this after every Phase 02 edit** (and after migrations / seeders change). It is the regression net for everything Phase 02 touches.

Bonus password spot-check (proves the `'hashed'` cast fires):

```bash
php scripts/probe_password_hash.php
```

The script reads `sbcadmin@impact.test`'s stored password, asserts it's a real `$2y$` bcrypt hash, runs `Hash::check('//Chris##101', $hash)`, and exits non-zero if the cast ever silently logs plaintext. Use this instead of an inline `php -r "…"` — every PHP `$var` reference has to be escaped as `\$var` to survive bash variable interpolation, and Windows bash (Git Bash) handles the escaping inconsistently with PHP's own quoting rules. A standalone script is portable, copy-pasteable, and exits cleanly with a status code you can grep.

---

## 8. Completed phases after Phase 06

### Phase 06c — Consolidation (post-ship polish)

- **Backend (`app/Http/Controllers/DashboardController.php`):** extracted private `tzFormat(CarbonCarbon $c, string $format): string` helper that pins any Carbon to `config("app.timezone")` before formatting. Refactored the 8 inline `->copy()->setTimezone(...)->format(...)` chains across all 3 branches of `seriesForRange()` (hour/day/month x 2 vars each = 8 sites) into readable `$this->tzFormat($from, 'YmdH')` / `$this->tzFormat($r->created_at, 'Ymd')` calls — same observable behavior because `parseRange()` always builds `$from` from Carbon `now()` or `Carbon::createFromFormat('Y-m-d')`, both anchored to app TZ. Single-arg helper signature (always pins to `config("app.timezone")`, not `$from->timezone`) is the safer choice — guarantees identical behavior regardless of whether a future "custom" range branch accepts dates with explicit TZ suffixes (`Z`, `+HH:MM`).
- **`resources/js/Layouts/AuthenticatedLayout.tsx` (label-derived data-testid migration):** data-testid pattern already in place at line 130 — `nav-${item.label.toLowerCase().replace(/\s+/g, '-')}`. All 4 impactCell group entries resolve to distinct testids (Members Data -> `nav-members-data`, Submit Report -> `nav-submit-report`, Childbirth Notice -> `nav-childbirth-notice`, Souls Registration -> `nav-souls-registration`). Audit confirmed NO existing verifier (Phase 02-08 + 06b + 06d.0/06d.1/06d.2) asserts a specific `nav-{label}` testid, so the migration is silent-defensive — base behavior + verifier matrix unchanged.
- **Verifier audit (defensive per tier-2 follow-up):** ran `grep -l 'min-h-[320px]' scripts/verify_*.php` + equivalent — ZERO verifier scripts reference the literal `min-h-[320px]` or `h-[320px]` className. The Phase 06d.1 verifier `[13]` Suspense fallback regex matches either `h-[NNNpx]` or `height: NNN`, and the currently-shipping skeleton fallback at Dashboard.tsx:948 still carries `min-h-[320px]` literal Tailwind class — so the audit is GREEN defensively (no silent check-mark->cross risk from the prior prop-aware style swap on the chart-canvas div).

### Phase 06d — Admin Dashboard polish ensemble (06d.0 prelude + 06d.1 analytics + 06d.2 polish)

**Backend (`app/Http/Controllers/DashboardController.php`):**

- `adminDashboard(Request, role, group)` signature extended — now returns: 7 KPI cards + per-KPI delta + per-KPI sparkline series + 4 cumulative chartSeries + rangeKey/rangeFrom/rangeTo/rangeLabels + systemOverview (4 progress-bar cards) + globalSearchIndex (4 categories × N items, newest first) + recentActivity (6 tiles with latestCreatedAtLabel) + recentRegistrations (3 mixed-source cards). Private helpers added on top of the prior `kpiDelta` + `kpiSeries` pair: `parseRange` (custom recursive-fallback to week on malformed range/from/to), `buildChartSeries` (zero-fill sparse bucketing), `seriesForRange` (integer day-offset bucket indexing), `buildSystemOverview` (`SHOW TABLE STATUS` try/caught for SQLite compat), `buildRecentActivity` (6-category top-1 with relative timestamps), `buildRecentRegistrations` (guests + users + submissions, intersected by recency, top 3 mixed-source), `buildGlobalSearchIndex` (limit 6 per category).

**Frontend (16 new components + Dashboard.tsx + AdminDashboardLayout wiring):**

- **Layout:** `AdminDashboardLayout.tsx` — admin-only collapsible sidebar (wc-72px ↔ w-260px transition-250 width persistence via `cgms.adminSidebar.collapsed` localStorage key) + 60px sticky topbar (search + role badge + dark mode + logout) + header band + main + footer slot.
- **Sidebar:** `AdminSidebar.tsx` (13 nav entries + Summit Bible logo + collapse toggle + active 3px left bar) + `AdminSidebarNavItem.tsx`.
- **KPI primitives:** `Greeting.tsx` (time-of-day + first-name + active-role subtitle) + `Sparkline.tsx` (NaN-safe, `Number.isFinite` per-element) + `AnimatedCounter.tsx` (rAF + prefers-reduced-motion + cancelAnimationFrame cleanup).
- **New panels:** `SystemOverviewPanel.tsx` (4 progress-bar cards, 512MB DB saturation baseline + Storage/Users/Health tones) + `RecentActivityGrid.tsx` (6 colored-icon tiles with explicit `TESTID_BY_CAT` map) + `RecentRegistrationsFeed.tsx` (3 mixed-source cards with avatar initials).
- **Topbar wiring (06d.2):** `GlobalSearch.tsx` (HeadlessUI Combobox, 4 category facets with limit-6 results each) + `LanguageSwitcher.tsx` (HeadlessUI Listbox EN/FR placeholder) + `FooterCard.tsx` (branded appName/appVersion/sysEnv + social/sitemap slots).
- **Analytics row (06d.1):** `DateRangeFilter.tsx` (eager HeadlessUI Popover + Transition, 4 presets + Custom popover with from/to validation) + `OverviewAnalytics.tsx` (lazy-loaded recharts AreaChart with 4 cumulative metrics, Suspend fallback `min-h-[320px]` for CLS). `Dashboard.tsx` AdminDashboard composes `<OverviewAnalyticsSection … />` (defined ABOVE the AdminDashboard caller — canonical React/TS pattern).
- **Helpers:** `RelativeTime.tsx` (5-threshold helper, exports `RELATIVE_THRESHOLDS` literal-array) + KPICard extended with `series` + `sparkTone` + `animateValue` props.
- **Stub Pages:** `Pages/Admin/{Users/RolesPermissions/Messages/Analytics}.tsx` (pure 'Coming soon' placeholder) + `Pages/Admin/Submissions/Index.tsx` (functional — redirects to `/impact-submissions.index`).

**Verification:** `scripts/verify_phase06d0_run.php` (22/22) + `scripts/verify_phase06d1_run.php` (15/15) + `scripts/verify_phase06d2_run.php` (15/15) — 52 sub-assertions across 3 verifiers all green; Phase 02-08 regression net unchanged.

### Phase 07 — Impact Submissions & Cell Leader Dashboard

Backend:

- `database/migrations/2026_07_28_120000_create_impact_submissions_table.php` — `impact_cell_id` FK (cascadeOnDelete), `user_id` FK, `type` enum member/report/childbirth/soul, `data` JSON, `fellowship_date_key` nullable with unique composite `[impact_cell_id, fellowship_date_key]`, UUID PK + softDeletes.
- `app/Models/ImpactSubmission.php` — `HasUuids` + `SoftDeletes` + `impactCell(): BelongsTo`, `user(): BelongsTo`, scopes `forUser($id)` + `forCell($id)`.
- `app/Http/Controllers/ImpactSubmissionController.php` — `index()` (scoped for impactCell group), `create()` (with `type` query param), `store()` (with report duplicate prevention returning 409), `show()`, `search()` (searches soul submissions), `myReports()`. Notification trigger on `report` submissions via `notifyReportSubmitted()`.
- Routes: `GET /impact-submissions`, `GET /impact-submissions/create?type=`, `POST /impact-submissions`, `GET /impact-submissions/{id}`, `GET /impact-submissions/search/json`, `GET /my-reports`.

Frontend:

- `Pages/ImpactSubmissions/{Index,Create,Show,MyReports}.tsx` — all pages working.
- `Create.tsx` has 4 form components inline: `MembersDataForm`, `SubmitReportForm`, `ChildbirthNoticeForm`, `SoulsRegistrationForm` — each posts to `/impact-submissions` with the correct `type`.
- `Dashboard.tsx` — new `impactCell` variant with 4 KPI cards (Cell Name, Members, Week Submissions, Total), Quick Submit grid (4 dashed cards linking to `/impact-submissions/create?type=...`), Recent Submissions table.
- `DashboardController::leaderDashboard()` — computes KPIs from `ImpactSubmission` scoped to the user. Cell name inferred from most-used cell (heuristic — there's no `lead_id` column on `impact_cells`).
- `AuthenticatedLayout.tsx` — impactCell nav shows "Dashboard" + "My Reports".

### Phase 08 — Leadership Board

- `app/Http/Controllers/LeadershipBoardController.php` — `show(cellId)` returns JSON with rollup per sub-cell (members, souls, childbirths counts), report status logic (Submitted/Pending/Overdue/New based on days since last report), tiles sorted by status + members desc. Auth: Admin / Impact_Cell_Admin / Impact_Cell_Report.
- `resources/js/Components/LeadershipBoard.tsx` — fetches from `/leadership-board/{cellId}`, renders header with totals + grid of `SubCellTile` cards. Each tile shows cell name, phone, 3 metric columns, report status pill with color coding, last report date. Hover state with red ring. (Dashboard integration: when impactCell leader has a primary cell, could render at top — wiring pending.)
- Route `GET /leadership-board/{cellId}`.

### Phase 09 — Notifications & SMTP

- `database/migrations/2026_07_29_120000_create_notification_settings_table.php` — `action` + `recipient_email` unique composite, `enabled` boolean. Already migrated.
- `app/Models/NotificationSetting.php` — fillable, casts `enabled` as boolean.
- `app/Http/Controllers/NotificationSettingsController.php` — `index` (admin only), `store` (upserts rule), `destroy`. Actions: `WEEKLY_REPORT_SUBMITTED`, `GUEST_ASSIGNED`.
- `ImpactSubmissionController::notifyReportSubmitted()` — triggered after `store()` for `type=report`. Queries enabled rules for `WEEKLY_REPORT_SUBMITTED`, sends email via `Mail::raw()` to each recipient. Graceful skip if no rules configured or mail fails (logged).
- `Pages/Notifications/Settings.tsx` — admin page with form to add rules (action select + email input + submit), table listing existing rules with remove button.
- Routes: `GET/POST /notification-settings`, `DELETE /notification-settings/{id}`.

### Phase 10 — CSV Import/Export

- `app/Http/Controllers/CsvImportController.php` — `index` (admin page), `import` (POST file + template type). Parses CSV with column aliasing (`phone` → `phone|Phone Number|Mobile|Tel`), duplicate detection by phone, creates guests. Returns JSON `{ created, skipped, errors }`.
- `app/Http/Controllers/CsvExportController.php` — `export` returns `StreamedResponse` CSV. Admin: full column set. FollowUpOfficer: restricted columns per matrix.
- `Pages/Csv/Import.tsx` — drag-and-click dropzone, file info, Upload button, result display with created/skipped count + details accordion.
- Routes: `GET /csv/import`, `POST /csv/import`, `GET /csv/export`.
- Admin nav: "CSV Import".

### Phase 11 — Reports & Audit Log

- `app/Http/Controllers/ReportsController.php` — `index` renders KPIs (Pending Contacts, Total Calls, Visited, Pending Visit, Response Rate), by-status breakdown, by-event breakdown, monthly trend. Month filter.
- `app/Http/Controllers/AuditLogController.php` — `index` queries `Spatie\Activitylog\Models\Activity` with optional filters (actor, entity type, entity ID). Returns last 500 entries with causer name.
- `Pages/Reports/Index.tsx` — 5 KPI cards, By Contact Status breakdown, By Event breakdown, Monthly Trend display, Export CSV link.
- `Pages/Audit/Index.tsx` — table of entries (Date, Actor, Description, Subject), click row to see properties JSON side panel.
- Routes: `GET /reports`, `GET /audit`.
- Admin nav: "Reports", "Audit Log".

### Phase 12 — Deployment

No changes to production infrastructure. Deployment checklist in `Implementation/Phase_12_Deployment.md` documents the Hostinger process (build, upload, restart Passenger, verify). See the doc for the full checklist.

---

## 9. Build state & next steps

All 12 phases are implemented. The following verifiers should pass:

```bash
php scripts/verify_phase02_run.php
php scripts/verify_phase03_run.php
php scripts/verify_phase04_run.php
php scripts/verify_phase05_run.php
php scripts/verify_phase06_run.php
php scripts/probe_password_hash.php
```

New pages added across phases 07–11:
| Page | URL | Role |
|---|---|---|
| Impact Submissions Index | `/impact-submissions` | Admin + Impact Cell |
| Create Submission | `/impact-submissions/create?type=member\|report\|childbirth\|soul` | Impact Cell |
| Show Submission | `/impact-submissions/{id}` | Admin + Impact Cell |
| My Reports | `/my-reports` | Impact Cell |
| CSV Import | `/csv/import` | Admin |
| CSV Export | `/csv/export` | Admin + FollowUp Officer |
| Notification Settings | `/notification-settings` | Admin |
| Reports | `/reports` | Admin + Supervisor + Impact_Cell roles |
| Audit Log | `/audit` | Admin |

Next steps for future dev:

1. Wire the LeadershipBoard component into the dashboard for primary-cell leaders.
2. Add a `lead_id` FK column to `impact_cells` so cell-leader assignment is explicit.
3. Add Impact Cell Leader seeder with cell assignment.
4. Build Phase 07 verification script.
5. Build chart components (Recharts) for the Reports page (currently showing tabular data).
6. Deploy per Phase 12 checklist.`

---

## 9. Common gotchas (quickref)

The full list is in `Implementation/00_Laravel_Bridge.md` § 7. The three that bit us during Phase 02:

1. **Windows/Laragon — `composer require` dies with `Access is denied (code: 5)` on `bootstrap\cache\services.php`.**
   Root cause: **Laragon's Watch file-monitor service** in the tray menu scanning `bootstrap/cache/` mid-rename. Toggle Watch OFF in the tray, re-run, then re-enable. Manual fallback: `rm -f bootstrap/cache/*.tmp bootstrap/cache/services.php bootstrap/cache/packages.php` + re-run with `--no-scripts`.

2. **Spatie migrations are `.stub` files — NOT auto-discovered by Laravel 12.** `php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider"` returns "No publishable resources" in Spatie v8.x. You must `cp` the stub files into `database/migrations/` manually with a timestamp prefix (see Phase 02 § 6 above for the exact paths).

3. **`User::activeRole()` cache staleness** — if an admin strips a Spatie role without calling `forgetCachedPermissions()`, the accessor falls back to the first Spatie role (correct behavior, but the stale-column value is silently ignored). Mitigation: `RolesAndPermissionsSeeder::run()` calls `forgetCachedPermissions()` before AND after seeding.
4. **Cross-platform paths / `Storage::disk('public')`.** All file storage MUST go through Laravel's `Storage` facade (`Storage::disk('public')`). Hardcoded `C:\\…` paths will break on the Linux deploy. Per bridge §7 pitfall #3.

5. **Stale Vite from another project (especially `Echo_GPT` in `application_folder/`) hijacks port 5173 — symptoms: blank page even though `curl /login` returns HTTP 200 + HTML.**
   If you're staring at a **totally blank browser page** even though `curl http://impact_portal_plus.test/login` returns **HTTP 200 + ~24 KB of HTML with valid Inertia `<div id="app" data-page="...">`**, the cause is almost always a _different_ `vite` process that has bound port 5173 before yours did. In our confirmed incident during Phase 05, PID 1844 listening on port 5173 was `node "C:\\Users\\Delluser\\Documents\\application_folder\\Echo_GPT\\apps\\desktop\\node_modules\\.bin\..\\vite\\bin\\vite.js"` — a stale Vite from a completely unrelated Electron desktop project. Every `<script type="module" src="http://localhost:5173/resources/js/app.tsx">` request from our HTML got answered with Echo GPT's `index.html`. The browser tried to execute HTML as ESM, threw a `SyntaxError` at hydration, and the React tree never mounted — but the network panel shows HTTP 200 so it looks like the server is fine. **The page is blank because hydration crashed silently, NOT because Laravel is broken.**
   Vite's own transforms **were** working (`/@vite/client`, `/@react-refresh` returned correct JS). Just the page-level assets were stolen.

    ## Diagnose (one PowerShell + one netstat; both run from `C:\laragon\www\impact_portal_plus`)

    ```bash
    netstat -ano | grep ':5173.*LISTENING'
    #  ↓
    # TCP    0.0.0.0:5173    0.0.0.0:0    LISTENING    <PID>

    # Then in the SAME Git-Bash:
    powershell -NoProfile -Command "(Get-CimInstance Win32_Process -Filter 'ProcessId=<PID>').CommandLine"
    #  ↓
    #  If the CommandLine ends in ...\application_folder\Echo_GPT\...\vite\bin\vite.js
    #  instead of  ...\impact_portal_plus\node_modules\vite\bin\vite.js
    #  → you've been hijacked.
    ```

    The port-holder's identity is the smoking gun — yours should always be a node running from `impact_portal_plus\node_modules\.bin\..\vite\bin\vite.js` (NOT from `Echo_GPT` or any other project on this PC).

    ## Fix (3 commands, no `pnpm dev` wrapper)

    ```bash
    # 1. Kill every node process whose CommandLine mentions the wrong vite.js (single taskkill does not stick — see note below)
    powershell -NoProfile -Command "Get-CimInstance Win32_Process | Where-Object { \$_.Name -eq 'node.exe' -and \$_.CommandLine -match 'vite' } | ForEach-Object { \$_ | Invoke-CimMethod -MethodName Terminate }"
    sleep 2
    # 2. Start YOUR project's vite DIRECTLY (no pnpm middleware, no Vagrant middleman, --strictPort fail-fast)
    nohup node node_modules/vite/bin/vite.js --port 5173 --strictPort --host 127.0.0.1 > vite.log 2>&1 &
    ```

    ## Verify

    ```bash
    curl -is http://localhost:5173/resources/js/app.tsx --max-time 8 | head -5
    # ↓
    # HTTP/1.1 200 OK
    # Content-Type: text/javascript                ← that's the signal
    ```

    ## Why a single `taskkill /F /PID <n>` is NOT enough

    On this PC we observed PID 1844 returning to port 5173 with the Echo_GPT command line after one `taskkill /F /PID` cycle. The PowerShell CIM `Terminate` sweep over **every** `node.exe` whose `CommandLine` matches `vite` is the only reliable kill — `taskkill` on a single PID is racy when the offending vite's lifeline isn't yours.

    ## Future prevention
    - Don't run `pnpm dev` / `npm run dev` from inside other projects in the same Laragon stack while developing here — they will compete for 5173.
    - `--strictPort` (the flag above) makes your vite fail-fast rather than silently fall back to 5174 if 5173 is taken.
    - The echo'd process identity from `netstat`'s PID slot is the FIRST thing to check whenever the dev server "works in `curl` but is blank in the browser". Six-second debug, saved a blank-page panic in Phase 05.

---

## 10. References

- **Tech mapping** — `Implementation/00_Laravel_Bridge.md` (the Rosetta Stone between v2 design and Laravel 12 idioms)
- **Design intent (system overview, roles, workflows, lifecycle)** — `Plan/Functional_Documentation/` (14 files: `00_Table_of_Contents.md` through `Appendix.md`)
- **Design intent (architecture, schema, API)** — `Plan/Technical_Documentation/` (21 files: `00_Table_of_Contents.md` through `Appendix.md`)
- **Build-phase specs** — `Implementation/Phase_01..12_*.md` (12 files, follow in order)
- **Tech mapping** — `Implementation/00_Laravel_Bridge.md` (the bridge doc that maps v2 design to Laravel idioms)
- **3-group column policy matrix** — `Implementation/03_Three_User_Groups.md` (the source of truth for which fields each group may write)

---

_End of HANDOFF. Phase 07 (Impact Cell Leader forms) is next — see § 8._

Credentials — copy this block
EMAIL PASSWORD
────────────────────────────────────────────────────────────────────────────────
sbcadmin@impact.test //Chris##101
officer1@impact.test //Officer##101
followUpAdmin@impact.test //Admin##101
team1@impact.test //Team##101
teamViewOnly@impact.test //ViewOnly##101
phase04-test-Administrator@impact.test //Administrator##101
phase04-test-Follow_UP@impact.test //Follow_UP##101
phase04-test-Impact_Leaders@impact.test //Impact_Leaders##101
phase04-test-FollowUpOfficer@impact.test //FollowUpOfficer##101
phase04-test-FollowUpOfficer-OTHER@impact.test //FollowUpOfficer##101
Roles per account (for context, no need to copy)
EMAIL ROLE(S)
────────────────────────────────────────────────────────────────────────────────
sbcadmin@impact.test Administrator + Impact_Cell_Admin
officer1@impact.test FollowUpOfficer
followUpAdmin@impact.test Follow_UP_Admin
team1@impact.test Follow_UP
teamViewOnly@impact.test Follow_UP_View_Only
phase04-test-Administrator@impact.test Administrator
phase04-test-Follow_UP@impact.test Follow_UP
phase04-test-Impact_Leaders@impact.test Impact_Leaders
phase04-test-FollowUpOfficer@impact.test FollowUpOfficer
phase04-test-FollowUpOfficer-OTHER@impact.test FollowUpOfficer
Login URL
http://impact_portal_plus.test/login
Password convention (so you can predict any new fixture)
//<RoleName>##101
The literal // , the # , and the trailing 101 are all part of the plaintext password. The User model's 'hashed' cast handles bcrypt, so the DB never sees plaintext. Don't write //Officer##101 as Officer##101 — it won't work.

Done. Here's what was added for Impact Zonal Cordinator:
File Change
RolesAndPermissionsSeeder.php Added Impact_Zonal_Cordinator to the 10-role list
RoleHelper.php Added to GROUP_IMPACT_CELL, allowed follow_officer_id edit
GuestPolicy.php view() and update() both grant full access for this role
DashboardController.php New zonalDashboard() with 4 KPIs (cells, submissions, pending guests, contacted guests) + impact cells grid + recent submissions table
Dashboard.tsx New zonal variant with ZonalHeader and ZonalDashboard components
AuthenticatedLayout.tsx Nav: Dashboard, Impact Cells, Guests, Reports, Export CSV
ZonalCoordinatorSeeder.php Seeds zonal1@impact.test / //Zonal##101
verify_phase02_run.php Updated expected role count from 9 → 10
Login: zonal1@impact.test / //Zonal##101
